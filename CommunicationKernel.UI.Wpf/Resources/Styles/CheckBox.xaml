<!--
    文件: Resources/Styles/CheckBox.xaml
    层级: UI 层 — WPF 主题
    作用: 深色勾选框（左方框 + 右文字）。颜色全部来自 SF.Brush.*。
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
      深色主题勾选框：左侧方框 + 右侧文字
      颜色全部来自 SF.Brush.*
    -->
    <Style x:Key="SF.Style.DarkCheckBox" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource SF.Brush.Text.Primary}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,16,10"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center"
                                Background="Transparent">
                        <Border x:Name="box"
                                Width="16" Height="16"
                                Background="{StaticResource SF.Brush.Bg.Hover}"
                                BorderBrush="{StaticResource SF.Brush.Border.Default}"
                                BorderThickness="1"
                                CornerRadius="3"
                                SnapsToDevicePixels="True"
                                VerticalAlignment="Center">
                            <Path x:Name="mark"
                                  Data="M 2.5,8 L 6.5,12 L 13,3.5"
                                  Stroke="{StaticResource SF.Brush.Accent.Default}"
                                  StrokeThickness="1.8"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  Visibility="Collapsed"
                                  Stretch="None"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                        </Border>
                        <ContentPresenter Margin="8,0,0,0"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="mark" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="box" Property="BorderBrush"
                                    Value="{StaticResource SF.Brush.Accent.Default}"/>
                            <Setter TargetName="box" Property="Background"
                                    Value="{StaticResource SF.Brush.Bg.Hover}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="box" Property="BorderBrush"
                                    Value="{StaticResource SF.Brush.Accent.Default}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="box" Property="Background"
                                    Value="{StaticResource SF.Brush.Bg.Pressed}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>