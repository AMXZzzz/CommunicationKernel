# CommunicationKernel 架构与调用流程

> 本文以**代码为准**描述整个解决方案的调用链路：每一个类是谁构造的、在什么时机构造、每个方法被谁调用、后台循环何时启动。
> 与 `规范.md`（架构纪律）的关系：`规范.md` 讲"为什么这样分层"，本文讲"运行时到底发生了什么"。
> 若本文与代码不一致，以代码为准并修正本文。

---

## 目录

0. [仓库级约定（行尾与文件类型）](#0-仓库级约定)
1. [项目与分层总览](#1-项目与分层总览)
2. [核心契约层（Core.Abstractions / Transport / Protocol）](#2-核心契约层)
3. [插件层（Plugin.Context + Plugins.*）](#3-插件层)
4. [路由层（Core.EngineRouter）](#4-路由层-coreenginerouter)
5. [引擎层（Core.EngineRuntime）](#5-引擎层-coreengineruntime)
6. [宿主层（Hosting.App）](#6-宿主层-hostingapp)
7. [客户端 SDK（Hosting.Sdk）](#7-客户端-sdk-hostingsdk)
8. [WPF 客户端（UI.Wpf）](#8-wpf-客户端-uiwpf)
9. [Web 客户端（UI.WebMaster）](#9-web-客户端-uiwebmaster)
10. [端到端时序：从点击到 PLC 字节](#10-端到端时序)
11. [后台循环一览表](#11-后台循环一览表)
12. [生命周期与释放顺序](#12-生命周期与释放顺序)

---

## 0. 仓库级约定

**行尾由 `.gitattributes` 强制，不依赖任何编辑器。** 默认 `* text=auto eol=crlf`（库内存 LF、检出转 CRLF），
与 `.editorconfig` 的 `end_of_line = crlf` 配套。三条例外：

| 例外 | 原因 |
|---|---|
| `*.sh` / `*.bash` / `Dockerfile` → `eol=lf` | **功能要求，不是风格。** 带 CRLF 传到 Linux 后 shebang 变成 `#!/usr/bin/env bash\r`，内核去找名叫 `bash\r` 的解释器，报 `bad interpreter`。`scripts/ck-panel.sh` 正是部署到树莓派上跑的 |
| `*.yml` / `*.yaml` → `eol=lf` | CI 跑在 Linux runner 上，与执行环境一致 |
| `CommunicationKernel.Tests/ApiBaselines/*.txt` → `eol=lf` | 机器生成：`PublicApiSurfaceTests` 以 `string.Join("\n", …)` 写出，固定 LF。若要求 CRLF，每次重生成基线都会翻转一次行尾 |

二进制类型（`*.ico *.exe *.dll *.ttf …`）显式声明为 `binary`；`*.cs *.razor *.xaml *.md` 显式声明为 `text`——
这些文件含大量中文，一旦被 git 的启发式误判为二进制，行尾转换会**直接损坏内容且不报错**。

**改 `.gitattributes` 必须同步改 `.editorconfig`**，只改一边会出现"编辑器写 LF、git 检出 CRLF"的拉锯，文件每切一次分支就变一次。

---

## 1. 项目与分层总览

```
┌──────────────────────────────────────────────────────────────────────────┐
│  UI 层（两个平行实现，功能不对等，刻意不合并）                                 │
│   CommunicationKernel.UI.Wpf            CommunicationKernel.UI.WebMaster   │
│   （独立进程，gRPC 连远端/本机 Host）      （把 Hosting.App 带进同一进程）      │
└───────────────┬──────────────────────────────────────┬───────────────────┘
                │ HostingClient (gRPC / HTTP2)          │ HostingClient → 127.0.0.1:{GrpcPort}
┌───────────────▼──────────────────────────────────────▼───────────────────┐
│  CommunicationKernel.Hosting.Sdk   —— DTO、HostingClient、ValueCodec、       │
│                                       JsonFileStore、RegisterFailure         │
└──────────────────────────────────┬───────────────────────────────────────┘
                                   │ protobuf: Protos/V1/hosting.proto
┌──────────────────────────────────▼───────────────────────────────────────┐
│  CommunicationKernel.Hosting.App   —— AppMain（独立进程入口）、               │
│                                       HostingComposition（组合根）、          │
│                                       HostingGrpcService（RPC → 引擎）        │
└──────────────────────────────────┬───────────────────────────────────────┘
┌──────────────────────────────────▼───────────────────────────────────────┐
│  CommunicationKernel.Core.EngineRuntime —— EngineRuntime（所有 PLC 状态与    │
│     任务的唯一持有者）、RouteAssembler、PluginRouteAssemblyService             │
└──────────────────────────────────┬───────────────────────────────────────┘
┌──────────────────────────────────▼───────────────────────────────────────┐
│  CommunicationKernel.Core.EngineRouter —— RouterOrchestrator、             │
│     ConnectionRouter（路由表）、ReadCoordinator（同键读合并）、               │
│     RouteEntry（每路由一把 I/O 锁）                                          │
└──────────────────────────────────┬───────────────────────────────────────┘
┌──────────────────────────────────▼───────────────────────────────────────┐
│  CommunicationKernel.Plugin.Context —— PluginCatalog / PluginLoadContext    │
│     （AssemblyLoadContext 隔离加载 plugins/*.dll）                            │
└───────────────┬────────────────────────────────────┬─────────────────────┘
                │                                    │
┌───────────────▼────────────────┐   ┌───────────────▼─────────────────────┐
│ CommunicationKernel.Core.Protocol│   │ CommunicationKernel.Core.Transport   │
│  IProtocolDriverFactory          │   │  ITransportFactory / ITransportClient│
│  IProtocolDriver / ProtocolProbe │   │  FrameReader / TryGetFrameLength     │
└───────────────┬────────────────┘   └───────────────┬─────────────────────┘
┌───────────────▼────────────────┐   ┌───────────────▼─────────────────────┐
│ Plugins.Protocol.Modbus          │   │ Plugins.Transport.Tcp                │
│ Plugins.Protocol.Siemens.S7      │   │ Plugins.Transport.SerialPort         │
│ Plugins.Protocol.Panasonic       │   │                                      │
└──────────────────────────────────┘   └──────────────────────────────────────┘
                     ▲ 全部依赖 ▲
        CommunicationKernel.Core.Abstractions（OperationResult / KernelErrorCode / KernelVersions）
```

**调用方向**（严格单向，禁止反向）：

```
UI → Hosting.Sdk → [gRPC] → Hosting.App → EngineRuntime → RouterOrchestrator
   → RouteEntry.ExecuteExclusiveAsync → IProtocolDriver（插件） → ITransportClient（插件）→ 物理链路
```

**关键约束**（详见 `.github/copilot-instructions.md`）：

- 协议解析只能在协议插件内部；EngineRuntime / Router / Hosting / UI 一律不知帧格式。
- Transport 不感知协议；协议通过 `TryGetFrameLength` 委托告诉传输"这一帧收全了没有"。
- 同一路由的读和写共用 `RouteEntry._ioGate` 一把锁。
- `length` 单位一律**字节**。

---

## 2. 核心契约层

### 2.1 `CommunicationKernel.Core.Abstractions`

| 类型 | 文件 | 说明 |
|---|---|---|
| `KernelErrorCode` | `Errors/KernelErrorCode.cs` | 枚举。通用 0-99：`None / Unknown / Timeout / Cancelled / InvalidArgument`；传输 100-199：`TransportUnavailable / TransportIoError`；协议 200-299：`ProtocolNotFound / ProtocolError`；路由 300-399：`RouteNotFound / RouteBusy`；插件 400-499：`PluginNotFound / PluginLoadFailed / PluginApiVersionMismatch / PluginIsolationError` |
| `OperationResult` | `Results/OperationResult.cs` | 构造函数 **private**，只能经静态工厂：`Ok()`、`Fail(message, code)`、`InvalidArgument(message)`、`Timeout(message)` |
| `OperationResult<T>` | `Results/OperationResultOfT.cs` | 同上：`Ok(value)`、`Fail(message, code)`、`From(OperationResult, value)` |
| `KernelVersions` | `Versioning/KernelVersions.cs` | `const int PluginApiVersion = 1`，插件装载时校验 |

### 2.2 `CommunicationKernel.Core.Transport`

全部在 `Abstractions/` 与 `Framing/`。

| 类型 | 关键成员 |
|---|---|
| `TransportKind` | `Tcp=0, Serial=1, Wifi=2, Bluetooth=3, Custom=99`。**只有 Tcp/Serial 有插件**。 |
| `TransportEndpoint` | `Kind, Address, Port, SerialPort?, BaudRate?, Properties` — 由 `RouteAssembler.BuildEndpoint()` 构造 |
| `ITransportFactory` | `TransportId`、`Kind`、`PluginApiVersion`、`ITransportClient CreateClient()` |
| `ITransportClient` | `IsConnectionAlive`、`ConnectAsync(endpoint, ct)`、`SendAndReceiveAsync(byte[] request, TryGetFrameLength, ct)`、`DisconnectAsync(ct)`、`DisposeAsync()` |
| `TryGetFrameLength` | `delegate bool (ReadOnlySpan<byte> received, out int totalLength)` — **协议插件提供**，传输层回调 |
| `ISerialPortEnumerator` / `SerialPortInfo` | `ListPorts()`；`record struct SerialPortInfo(PortName, Description)`（引擎侧命名） |
| `FrameReader` | 构造 `FrameReader(int maxResponseBytes, int firstByteTimeoutMs, int subsequentByteTimeoutMs, string mediumName)`；`DiscardResidual()`；`ReadFrameAsync(Stream, TryGetFrameLength, ct)`。**每个传输客户端持有一个实例**。 |

### 2.3 `CommunicationKernel.Core.Protocol`

| 类型 | 关键成员 |
|---|---|
| `ProtocolMetadata` | `ProtocolId, DisplayName, SupportedTransports, RequiresStation, StationHint, PluginApiVersion` |
| `ProtocolDriverContext` | `Station`（站号只在这里传入，地址里不接受 `1:40001` 前缀） |
| `IProtocolDriverFactory` | `Metadata`、`CreateDriver(ProtocolDriverContext? context = null)` |
| `IProtocolDriver` | `BuildReadFrame(address, length)`、`BuildWriteFrame(address, payload)`、`ReadAsync(ITransportClient, address, length, ct)`、`WriteAsync(ITransportClient, address, payload, ct)`、`ProbeAsync(ITransportClient, ct)` |
| `ProtocolProbe` | 静态 `ReadAsync(driver, client, address, length, ct)` — 引擎注册路由后做的一次探活 |

---

## 3. 插件层

### 3.1 `CommunicationKernel.Plugin.Context` — 加载机制

| 类型 | 文件 | 构造 / 方法 | 谁调用 |
|---|---|---|---|
| `PluginKind` | `Abstractions/PluginKind.cs` | `Transport=0, Protocol=1` | — |
| `PluginDescriptor` | `Abstractions/PluginDescriptor.cs` | `required PluginId / DisplayName / Kind / ApiVersion`；可选 `Version / EntryType / AssemblyPath` | 各插件 Manifest |
| `IPluginManifest` | `Abstractions/IPluginManifest.cs` | `Descriptor { get; }` | 每个插件 DLL 必须恰有一个实现 |
| `PluginLoadResult` | `Loader/PluginLoadResult.cs` | `Descriptor / Assembly / LoadContext` | `PluginCatalog` 返回 |
| `PluginLoadContext` | `Loader/PluginLoadContext.cs` | `: AssemblyLoadContext(name, isCollectible: true)`，构造 `(string assemblyPath)`，内含 `AssemblyDependencyResolver`；`Load(AssemblyName)` 先在插件目录解析私有依赖 | `PluginCatalog.TryLoadPlugin` |
| `PluginCatalog` | `Loader/PluginCatalog.cs` | 构造 `(ILogger<PluginCatalog>? logger)`；`IReadOnlyList<PluginLoadResult> DiscoverAndLoad(string pluginDirectory)` | `PluginRouteAssemblyService.LoadFactories()`（**进程内只调一次**） |

**`PluginCatalog.DiscoverAndLoad` 流程**：

```
遍历 pluginDirectory 顶层 *.dll
  └─ TryLoadPlugin(path)
       ├─ new PluginLoadContext(path)
       ├─ ctx.LoadFromAssemblyPath(path)
       ├─ 找 IPluginManifest 实现 → Activator.CreateInstance
       ├─ 校验 Descriptor.ApiVersion == KernelVersions.PluginApiVersion
       ├─ 成功 → 保留 ALC（不卸载），返回 PluginLoadResult
       └─ 失败 → ctx.Unload()，记日志，继续下一个 DLL（单个坏插件不拖垮整体）
```

插件**更新策略是重启生效**：ALC 虽是 collectible，但运行期不做热卸载。

### 3.2 传输插件

#### `Plugins.Transport.Tcp` — `TcpTransportPlugin.cs`

| 类 | 构造 | 关键方法 |
|---|---|---|
| `TcpTransportPluginManifest : IPluginManifest` | 无参 | `PluginId="transport-tcp"`, `Kind=Transport`, `ApiVersion=1` |
| `TcpTransportFactory : ITransportFactory` | 无参 | `TransportId="transport-tcp"`, `Kind=Tcp`, `CreateClient() → new TcpTransportClient()` |
| `TcpTransportClient : ITransportClient` | 无参；内部 `new FrameReader(1024, 5000, 1000, "TCP")` | 见下 |

`TcpTransportClient` 调用顺序：

```
ConnectAsync(endpoint, ct)
  ├─ _tcp = new TcpClient { NoDelay = true }
  ├─ await _tcp.ConnectAsync(endpoint.Address, endpoint.Port, ct)
  ├─ _stream = _tcp.GetStream()
  └─ EnableKeepAlive(_tcp.Client)

SendAndReceiveAsync(request, tryGetFrameLength, ct)      ← 由协议驱动调用，已处于 RouteEntry 锁内
  ├─ if (_desynced) return Fail(TransportIoError)          // 已知失步，必须先重连
  ├─ bool requestSent = false
  ├─ _frameReader.DiscardResidual()                       // 丢上一帧残留（FrameReader 自己的缓冲）
  ├─ DrainSocketBuffer()                                  // 丢 socket 内核缓冲里的陈旧字节
  ├─ await _stream.WriteAsync(request, ct); requestSent = true
  ├─ frame = await _frameReader.ReadFrameAsync(...)
  ├─ if (frame.Success) requestSent = false
  └─ finally { if (requestSent) _desynced = true; }        // 发了没收到 → 标记失步
```

**`_desynced` 是这套防护的关键一半**，不能只看前两行的清缓冲：

- 清缓冲只能丢掉**已经到达**的字节。若请求已发出、响应还在路上时读取被取消（切页面、停轮询、超时、退出），
  那个响应会在之后才抵达并躺进内核缓冲——下一次请求发出后**先读到的就是它**，从此请求与响应永久错位一格。
- Modbus TCP 有事务 ID，错位会被认出来（`事务 ID 不匹配`），但 `ProtocolError` 不在重连判据里，这条路由再也无法自愈。
- **MEWTOCOL 没有事务 ID**：同样的错位不会报任何错，只会让每个寄存器值都变成"上一次读到的那个"——
  帧完整、BCC 也对，校验和拦不住。

因此改为：一旦"发出去了但没读到响应"就置 `_desynced`，后续请求直接以 `TransportIoError` 失败
（该错误码在 `EngineRuntime.ShouldAttemptReconnect` 里，会触发重连）；`ConnectAsync` 与 `DisposeInternals` 清零该标记。
由 `Tests/TcpCancelDesyncTests` 锁定。串口侧同构，只是清缓冲用 `DiscardInBuffer()`。

#### `Plugins.Transport.SerialPort` — `SerialPortTransportPlugin.cs`

| 类 | 构造 | 关键方法 |
|---|---|---|
| `SerialPortTransportPluginManifest` | 无参 | `PluginId="transport-serial"` |
| `SerialPortTransportFactory : ITransportFactory, ISerialPortEnumerator` | 无参 | `TransportId="transport-serial"`, `Kind=Serial`, `CreateClient()`, `ListPorts()`（**宿主机器上的口**，UI 经 `QuerySerialPorts` 拿） |
| `SerialPortTransportClient` | 无参；`new FrameReader(1024, 3000, 500, "串口")` | `ConnectAsync` 读 `endpoint.SerialPort / BaudRate / Properties[Parity, DataBits, StopBits]`；`SendAndReceiveAsync` 同 TCP 流程 |

### 3.3 协议插件

所有协议驱动的 `ReadAsync` 都遵循同一骨架（**协议解析只在这里**）：

```
ReadAsync(client, address, length, ct)
  ├─ BuildReadFrame(address, length)                 // 地址解析 + 组帧；length 是字节，插件自换算
  │     失败 → 返回 Fail(InvalidArgument)，不发 I/O
  ├─ await client.SendAndReceiveAsync(frame, TryGetFrameLength, ct)
  └─ 解析响应 → OperationResult<byte[]>（大端字节流上抛，UI 侧用 ValueCodec 按设备 ByteOrder 换算）
```

| 插件 / 文件 | Factory | ProtocolId | 支持介质 | RequiresStation | Driver 构造 |
|---|---|---|---|---|---|
| Modbus `ModbusTcpPlugin.cs` | `ModbusTcpProtocolDriverFactory` | `modbus-tcp` | Tcp | ✔ (1-247) | `new ModbusProtocolDriver(meta, new ModbusTcpEnvelope(), unitId)` |
| Modbus `ModbusRtuPlugin.cs` | `ModbusRtuProtocolDriverFactory` | `modbus-rtu` | Serial, Tcp | ✔ | `… new ModbusRtuEnvelope() …` |
| Modbus `ModbusAsciiPlugin.cs` | `ModbusAsciiProtocolDriverFactory` | `modbus-ascii` | Serial, Tcp | ✔ | `… new ModbusAsciiEnvelope() …` |
| S7 `S7Plugin.cs` | `SiemensS7_1200ProtocolDriverFactory` | `siemens-s7-1200` | Tcp | ✘（Rack/Slot 在 TSAP） | `new SiemensS7ProtocolDriver(meta, remoteTsap: 0x0300)` |
| S7 `S7Plugin.cs` | `SiemensS7_200SmartProtocolDriverFactory` | `siemens-s7-200smart` | Tcp | ✘ | `… remoteTsap: 0x0200` |
| Panasonic `MewtocolPlugin.cs` | `MewtocolProtocolDriverFactory` | `panasonic-mewtocol` | Serial, Tcp | ✔ (1-99) | `new MewtocolProtocolDriver(meta, MewtocolAddress.ResolveDefaultStation(context?.Station))` |

**Modbus 内部**：`ModbusProtocolDriver` 把"功能码/地址/寄存器计数"与"信封"（MBAP / CRC16 / LRC）分开，三个 Factory 只换信封。
**S7 内部**：`ReadAsync` 先 `EnsureHandshakeAsync`（COTP CR + S7 SetupCommunication，仅首次），再 `S7Frame.BuildReadVar` → `SendAndReceiveAsync` → `S7Frame.ParseReadResponse`。
**Mewtocol 内部**：`PlanRead` 判断位地址/单字 → `RCS`，多字 → `RD`（上限 128 字）。
响应侧**必须先验 BCC 再解释内容**（`MewtocolFrame.VerifyBcc`，在 `ParseReadContactResponse` /
`ParseReadDataResponse` / `ParseWriteResponse` 三处入口）：校验失败一律 `ProtocolError`，
包括帧自称是错误响应（`!42`）的情况——整帧不可信时，里面的错误码同样不可信。
`**` / `!!` 是规范允许的"本帧不校验"占位，须放行。
此前接收侧完全不校验（注释原文「忽略 BCC 和 CR」），RS-485 长线上被噪声打翻一位的响应会被当作有效寄存器值显示，
且标着"成功"。由 `Tests/MewtocolResponseBccTests` 用人为篡改的合成帧锁定——不接真设备就没有噪声，自测覆盖不到。

---

## 4. 路由层 `Core.EngineRouter`

命名空间约定：`Abstractions/` → `.Abstractions`（只有接口）；`Models/` → `.Models`；`Runtime/` → 根命名空间 `CommunicationKernel.Core.EngineRouter`。

### 4.1 `Models/RouteKey`
物理连接唯一键：`ProtocolId + TransportKind + Address + Port + Station`（TCP）或 `SerialPort`（串口）。两条 RouteId 指向同一物理连接时，第二条注册失败 `RouteConflict`。

### 4.2 `Runtime/RouteEntry.cs` — `sealed class RouteEntry : IAsyncDisposable`

```csharp
public required RouteKey          Key             { get; init; }
public required ITransportClient  TransportClient { get; init; }
public required IProtocolDriver   ProtocolDriver  { get; init; }
public          int               MinIoIntervalMs { get; init; }
private readonly SemaphoreSlim    _ioGate = new(1, 1);          // 读写共用
private long _lastIoCompletedUtcTicks;
```

- **构造者**：`RouteAssembler.AssembleAsync`（对象初始化器）。
- `ExecuteExclusiveAsync<T>(Func<CancellationToken, Task<T>> ioAction, ct)`：
  1. `await _ioGate.WaitAsync(ct)`
  2. 若 `MinIoIntervalMs > 0`：`delay = MinIoIntervalMs - 距上次 I/O 完成毫秒数`，`> 0` 则 `Task.Delay`（**串口帧间静默在这里**）
  3. `await ioAction(ct)`
  4. `finally`：记录完成时刻，`_ioGate.Release()`
  调用者：`EngineRuntime.ExecuteWithRoutePolicyAsync`（读/写）、`RecoverDeadLinkAsync`、`ProbeIdleRouteAsync`。
- `HasCompletedIoWithin(int ms)`：`EngineRuntime.InspectRouteLinkAsync` 用于跳过刚活跃过的路由的心跳。
- `DisposeAsync()`：`TransportClient.DisposeAsync()`，吞异常。调用者：`RouterOrchestrator.TryRemoveAndDisposeAsync`、`RouteAssemblyResult.RollbackAsync`。
- `_ioGate` **刻意不 Dispose**，避免在途 `Release` 抛 `ObjectDisposedException`。

### 4.3 `Runtime/ConnectionRouter.cs` — `IConnectionRouter`

- 构造：`ConnectionRouter(ILogger<ConnectionRouter>? logger = null)`；由 `HostingComposition.AddServices` 以 lambda 注册为 Singleton。
- 数据：`ConcurrentDictionary<RouteKey, RouteEntry> _routes`。
- `Count`、`Snapshot()`、`TryRegister(entry)`（`TryAdd`，拒绝覆盖）、`TryGet(key, out entry)`、`TryRemove(key, out entry)`（**只摘表不释放**）。

### 4.4 `Runtime/ReadCoordinator.cs` — `IReadCoordinator`

- 构造：无参（DI 直接 `AddSingleton<IReadCoordinator, ReadCoordinator>()`）。
- 数据：`ConcurrentDictionary<ReadRequestKey, InflightRead> _inflight`；`ReadRequestKey = (RouteKey, Address, Length)`。
- `ExecuteAsync(key, Func<CancellationToken, Task<OperationResult<byte[]>>> readAction, ct)`：
  1. `Join(key, readAction)` — 取或建 `InflightRead`，`TryEnter()` 原子增参与者计数
  2. `await inflight.Task.WaitAsync(ct)` — 多个同键并发读共享一次真实 I/O
  3. 调用方取消 → `Leave()`，参与者归零才取消底层 CTS；其他参与者不受影响
- `InflightRead`：`Lazy<Task>` 延迟启动；独立 `_cts`；`RunAsync` 完成后 `Volatile.Write(completed)` 并按条件从 `_inflight` 移除。

### 4.5 `Runtime/RouterOrchestrator.cs` — `IRouterOrchestrator`

- 构造：`RouterOrchestrator(IConnectionRouter connectionRouter, IReadCoordinator readCoordinator)`；DI `AddSingleton<IRouterOrchestrator, RouterOrchestrator>()`。
- 纯转发门面：

| 方法 | 转发到 |
|---|---|
| `RouteCount` | `_connectionRouter.Count` |
| `TryRegister(RouteEntry)` | `_connectionRouter.TryRegister` |
| `TryGet(RouteKey, out RouteEntry?)` | `_connectionRouter.TryGet` |
| `TryRemoveAndDisposeAsync(RouteKey, ct)` | `_connectionRouter.TryRemove` → `entry.DisposeAsync()` |
| `ExecuteReadAsync(ReadRequestKey, Func, ct)` | `_readCoordinator.ExecuteAsync` |

---

## 5. 引擎层 `Core.EngineRuntime`

### 5.1 `PluginRouteAssemblyService : IRouteAssemblyService`

```csharp
public PluginRouteAssemblyService(string pluginDirectory, int defaultSerialMinIoIntervalMs = 15, ILoggerFactory? loggerFactory = null)
```

- **构造时机**：`HostingComposition.AddServices` 注册 lambda；**首次解析**发生在 `HostingComposition.Warmup` 里 `GetRequiredService<EngineRuntime>()`（EngineRuntime 依赖它）。
- 构造函数内立即执行 `LoadFactories()`：
  ```
  new PluginCatalog(loggerFactory?.CreateLogger<PluginCatalog>())
    .DiscoverAndLoad(pluginDirectory)
  遍历每个 PluginLoadResult.Assembly.GetTypes()
    ├─ 实现 ITransportFactory      → Activator.CreateInstance → transportFactories.Add
    └─ 实现 IProtocolDriverFactory → Activator.CreateInstance → protocolFactories.Add
    （单个工厂实例化失败只记日志）
  _assembler = new RouteAssembler(transportFactories, protocolFactories, defaultSerialMinIoIntervalMs, logger)
  ```
- 公开方法：
  - `GetAvailableProtocols()` → `IReadOnlyList<ProtocolMetadata>`（`HostingGrpcService.QueryProtocols`、`Warmup` 调用）
  - `GetAvailableSerialPorts()` → `SerialPortDiscovery.Enumerate(transportFactories)`，对实现了 `ISerialPortEnumerator` 的工厂求并集去重（`HostingGrpcService.QuerySerialPorts` 调用）
  - `AssembleAsync(RegisterRouteCommand, ct)` → 委托 `_assembler`

`StaticRouteAssemblyService` 是同接口的测试用实现，直接注入工厂列表，不扫目录。

### 5.2 `RouteAssembler`

```csharp
public RouteAssembler(IReadOnlyList<ITransportFactory> transportFactories,
                      IReadOnlyList<IProtocolDriverFactory> protocolFactories,
                      int defaultSerialMinIoIntervalMs, ILogger logger)
```

`AssembleAsync(RegisterRouteCommand cmd, ct)` → `OperationResult<RouteAssemblyResult>`，**这是"注册即连接"的地方**：

```
 1. 按 cmd.ProtocolId 找协议工厂          → 无 → Fail(ProtocolNotFound)
 2. 解析 cmd.TransportKind 枚举          → 非法 → Fail(InvalidArgument)
 3. 校验 meta.SupportedTransports 含该介质 → 否 → Fail(InvalidArgument)
 4. 按 Kind（+ 可选 TransportId）找传输工厂 → 无 → Fail(TransportUnavailable)   ← 插件没装
 5. 构建 RouteKey
 6. BuildEndpoint(cmd) → TransportEndpoint
 7. client = transportFactory.CreateClient();  await client.ConnectAsync(endpoint, ct)
        失败 → client.DisposeAsync(); Fail(TransportIoError / Timeout)           ← PLC 不可达
 8. driver = protocolFactory.CreateDriver(new ProtocolDriverContext { Station = cmd.Station })
 9. minIo = cmd.MinIoIntervalMs > 0 ? cmd.MinIoIntervalMs : (isSerial ? defaultSerialMinIoIntervalMs : 0)
10. entry = new RouteEntry { Key, TransportClient = client, ProtocolDriver = driver, MinIoIntervalMs = minIo }
11. RollbackAsync = ct => entry.DisposeAsync()      // 后续步骤失败时由 EngineRuntime 调用
12. return Ok(new RouteAssemblyResult { RouteEntry, RouteKey, Endpoint, TransportId, IsSerialRoute, MinIoIntervalMs, RollbackAsync })
```

### 5.3 `EngineRuntime : IAsyncDisposable` — 所有 PLC 状态与任务的唯一持有者

```csharp
public EngineRuntime(IRouteAssemblyService routeAssemblyService,
                     IRouterOrchestrator orchestrator,
                     ILogger<EngineRuntime>? logger = null,
                     int linkCheckIntervalMs = 5000)
```

**构造时做的事**：
1. `TryEnterProcess()` — 进程级互斥（一个进程只允许一个 EngineRuntime；测试宿主放行）
2. 若 `linkCheckIntervalMs > 0`：`_linkCheckCts = new(); _linkCheckTask = Task.Run(() => LinkCheckLoopAsync(...))` — **链路检查循环从构造函数即启动**

**构造时机**：`HostingComposition.Warmup(app)` 中 `app.Services.GetRequiredService<EngineRuntime>()`，即 `builder.Build()` 之后、`app.Run()` 之前。

**字段**：

| 字段 | 类型 | 含义 |
|---|---|---|
| `_registrationsByRouteId` | `ConcurrentDictionary<string, RouteRuntimeRegistration>` | RouteId → 运行时登记（含 `RouteEntry`、`Endpoint`、`RouteKey`） |
| `_routeIdByKey` | `ConcurrentDictionary<RouteKey, string>` | 物理连接唯一性 |
| `_routeStatuses` | `ConcurrentDictionary<string, RouteStatusSnapshot>` | 最近一次状态快照 |
| `_routeIdReservations` | `ConcurrentDictionary<string, byte>` | 注册中占位，防并发重复注册 |
| `RouteStatusChanged` | `event Action<RouteStatusSnapshot>?` | 唯一对外状态推送出口 |

**公开方法与内部调用链**：

`RegisterRouteAsync(RegisterRouteCommand, ct)` → `OperationResult<string>`：
```
 1. _routeIdReservations.TryAdd(routeId)            失败 → Fail(RouteConflict)
 2. assembled = await _routeAssemblyService.AssembleAsync(cmd, ct)
        失败 → 释放占位；Fail(原错误码)
 3. _routeIdByKey.TryAdd(RouteKey, routeId)        失败 → RollbackAsync；Fail(RouteConflict)
 4. _orchestrator.TryRegister(entry)               失败 → 回滚 3；RollbackAsync；Fail(RouteConflict)
 5. _registrationsByRouteId[routeId] = new RouteRuntimeRegistration(...)
 6. PublishStatus(routeId, online: true, None)      → RaiseStatusChanged
 7. return Ok(routeId)
```

`UnregisterRouteAsync(routeId, ct)`：
```
 _registrationsByRouteId.TryRemove → 无 → Fail(RouteNotFound)
 _routeIdByKey.TryRemove; _routeIdReservations.TryRemove
 await _orchestrator.TryRemoveAndDisposeAsync(key, ct)     // 摘表 → DisposeAsync 关 socket/串口
 PublishFinalOffline(routeId)                                // 最后一条离线事件并移除快照
```

`ReadByRouteIdAsync(routeId, address, length, ct)` → `OperationResult<byte[]>`：
```
 registration = _registrationsByRouteId[routeId]            无 → Fail(RouteNotFound)
 return _orchestrator.ExecuteReadAsync(
     new ReadRequestKey(key, address, length),               // 同键并发读合并
     ct => ExecuteWithRoutePolicyAsync(registration,          // 进 RouteEntry 锁
              ct2 => registration.RouteEntry.ProtocolDriver.ReadAsync(
                        registration.RouteEntry.TransportClient, address, length, ct2),
              ct),
     ct)
```

`WriteByRouteIdAsync(routeId, address, byte[] data, ct)` → `OperationResult`：
```
 同上查表 → ExecuteWithRoutePolicyAsync(registration,
     ct2 => ProtocolDriver.WriteAsync(TransportClient, address, data, ct2), ct)   // 不经 ReadCoordinator
```

`ExecuteWithRoutePolicyAsync`（读/写各一个重载）= `registration.RouteEntry.ExecuteExclusiveAsync(ct => Run{Read|Write}WithPolicyAsync(...), ct)`。

`Run{Read|Write}WithPolicyAsync` 策略（**只重试一次**）：
```
 result = await ioAction(ct)
 成功 → PublishStatus(online: true) → 返回
 ShouldAttemptReconnect(result.ErrorCode)   // TransportIoError | TransportUnavailable | Timeout；Cancelled 刻意不算
   → TryReconnectAsync(registration)        // 先 DisconnectAsync 再 ConnectAsync(原 Endpoint 副本)
   → 成功则在同一锁内立即重试一次 ioAction → PublishStatus(按重试结果)
 否则 PublishStatus(online: false, 原错误) → 返回原 result
 任何未包装异常 → PublishStatus(offline, TransportIoError) → Fail(TransportIoError)
```

`SnapshotRoutes()` → `IReadOnlyList<RouteRuntimeInfo>`（`QueryRoutes` RPC）。
`SnapshotStatuses(routeId?)`（`WatchRouteStatus` RPC 首帧）。
`RouteCount` → `_orchestrator.RouteCount`；`PendingRouteCount` → 占位数 − 已登记数。

**后台链路检查**（`LinkCheckLoopAsync(intervalMs, ct)`，默认 5s 一轮）：
```
 foreach registration in _registrationsByRouteId
   InspectRouteLinkAsync(registration)
     ├─ !TransportClient.IsConnectionAlive
     │     → RecoverDeadLinkAsync：ExecuteExclusiveAsync(TryReconnectAsync)；失败 PublishStatus(offline)
     └─ 活着 且 !RouteEntry.HasCompletedIoWithin(intervalMs)
           → ProbeIdleRouteAsync：ExecuteExclusiveAsync(driver.ProbeAsync(transport))
                成功 PublishStatus(online)；失败 → 走重连
```
因此**没有轮询也能发现掉线**，UI 状态灯来自 `RouteStatusChanged`。

`PublishStatus(routeId, online, code, message)`：原子比较旧快照，**有变化才** `RaiseStatusChanged`；`RaiseStatusChanged` 逐订阅者隔离异常。

`DisposeAsync()`：取消链路检查 → 注销全部路由 → `LeaveProcess()`。

---

## 6. 宿主层 `Hosting.App`

### 6.1 `HostingComposition`（静态组合根，独立进程与内嵌两种场景共用）

| 方法 | 内容 |
|---|---|
| `AddServices(IServiceCollection, IConfiguration)` | `services.AddGrpc()`；<br>`AddSingleton<IRouteAssemblyService>(sp => new PluginRouteAssemblyService(pluginDir, defaultSerialMinIoIntervalMs, loggerFactory))` — `pluginDir` 取 `config["Plugins:Directory"]`，缺省 `{BaseDirectory}/plugins`；<br>`AddSingleton<IConnectionRouter>(sp => new ConnectionRouter(logger))`；<br>`AddSingleton<IReadCoordinator, ReadCoordinator>()`；<br>`AddSingleton<IRouterOrchestrator, RouterOrchestrator>()`；<br>`AddSingleton<EngineRuntime>()`（构造参数由 DI 解析，`linkCheckIntervalMs` 走默认 5000） |
| `ListenGrpc(ListenOptions)` | `options.Protocols = HttpProtocols.Http2` |
| `MapEndpoints(WebApplication)` | `app.MapGrpcService<HostingGrpcService>()` |
| `Warmup(WebApplication)` | `GetRequiredService<EngineRuntime>()`（**触发插件扫描与链路循环启动**）→ `GetRequiredService<IRouteAssemblyService>().GetAvailableProtocols()` 返回协议 Id 列表供日志 |

### 6.2 `AppMain.Main(string[] args)` — 独立宿主进程

```
 1. Mutex(HostingComposition.InstanceMutexName) 抢占；失败 → ReportDuplicateInstance()（MessageBoxW）退出
 2. builder = WebApplication.CreateBuilder(args)
 3. builder.WebHost.ConfigureKestrel(o => o.ListenAnyIP(port, HostingComposition.ListenGrpc))
 4. HostingComposition.AddServices(builder.Services, builder.Configuration)
 5. app = builder.Build()
 6. HostingComposition.Warmup(app)                       ← EngineRuntime 在此构造
 7. HostingComposition.MapEndpoints(app)
 8. app.MapGet("/", …)                                    // 浅健康页
 9. app.Lifetime.ApplicationStarted.Register(...)         // 非 loopback 监听时打警告
10. app.Run()                                             // 阻塞；异常 → IsAddressInUse 判端口占用给出提示
```

### 6.3 `Services/Grpc/HostingGrpcService : HostingApi.HostingApiBase`

```csharp
public HostingGrpcService(EngineRuntime hostRuntime, IRouteAssemblyService routeAssemblyService, ILogger<HostingGrpcService> logger)
```
gRPC 服务默认 **per-call** 生命周期：每次 RPC 新建实例，但注入的三者都是 Singleton。

| RPC | 映射 |
|---|---|
| `Health` | `_hostRuntime.RouteCount` + 版本 |
| `GetDiagnostics` | `RouteCount` + `PendingRouteCount` |
| `RegisterRoute` | protobuf → `RegisterRouteCommand` → `_hostRuntime.RegisterRouteAsync(cmd, context.CancellationToken)` |
| `RemoveRoute` | `_hostRuntime.UnregisterRouteAsync` |
| `QueryRoutes` | `_hostRuntime.SnapshotRoutes()` + 按 RouteId/ProtocolId/TransportKind/Address 过滤 |
| `QueryProtocols` | `_routeAssemblyService.GetAvailableProtocols()` → `ProtocolDescriptor` |
| `QuerySerialPorts` | `_routeAssemblyService.GetAvailableSerialPorts()` → `SerialPortDescriptor`（gRPC 侧命名） |
| `Read` | `_hostRuntime.ReadByRouteIdAsync` |
| `Write` | `_hostRuntime.WriteByRouteIdAsync` |
| `WatchRouteStatus`（server streaming） | `Channel.CreateBounded<RouteStatusSnapshot>`；`_hostRuntime.RouteStatusChanged += OnStatus`；先推 `SnapshotStatuses(request.RouteId)` 作首帧；循环 `channel.Reader.ReadAsync(ct)` → `responseStream.WriteAsync`；`finally` 退订并 `TryComplete` |

---

## 7. 客户端 SDK `Hosting.Sdk`

### 7.1 `HostingClient : IHostingClient, IAsyncDisposable`

```csharp
public HostingClient(string serverAddress, ILogger<HostingClient>? logger = null)
```
构造即 `GrpcChannel.ForAddress(serverAddress)` + `new HostingApi.HostingApiClient(channel)`。

| 方法 | Deadline | 返回 |
|---|---|---|
| `HealthAsync(ct)` | 5s | `HealthResultDto(Ok, HostVersion, RouteCount)`；网络异常 → `HealthResultDto.Offline()` |
| `RegisterRouteAsync(routeId, protocolId, transportKind, address, port, station, serialPort="", baudRate=0, minIoIntervalMs=100, ct)` | 30s | `RegisterRouteResultDto`。**注意 SDK 默认 100 是正数，引擎会原样采用；UI 必须显式传设备配置值（0 表示交给引擎按介质取默认）** |
| `RemoveRouteAsync(routeId, ct)` | 30s | `RemoveRouteResultDto` |
| `QueryRoutesAsync(routeId="", protocolId="", transportKind="", address="", ct)` | 10s | `IReadOnlyList<RouteDto>` |
| `QueryProtocolsAsync(ct)` | 10s | `IReadOnlyList<ProtocolDescriptorDto>` |
| `QuerySerialPortsAsync(ct)` | 10s | `IReadOnlyList<SerialPortDto>`（SDK 侧命名） |
| `ReadAsync(routeId, dataAddress, length, ct)` | 10s | `ReadResultDto(…, byte[] Data)` |
| `WriteAsync(routeId, dataAddress, byte[] data, ct)` | 10s | `WriteResultDto` |
| `WatchRouteStatusAsync(routeId, Func<RouteStatusDto,Task> onStatus, Func<Task>? onDisconnected=null, ct)` | 无 | 循环读服务器流直到取消/断流 |
| `DisposeAsync()` | — | `channel.ShutdownAsync()` + Dispose |

RPC 异常统一转 `ErrorCode = "RPC_ERROR"` / `"TIMEOUT"`（客户端自填码）。

### 7.2 结果与 DTO（`HostingResults.cs`）

```
record HostingOperationResult(bool Success, string ErrorCode, string ErrorMessage)   // Ok() / Fail(code, msg)
   ├─ RegisterRouteResultDto(+ RouteId)
   ├─ RemoveRouteResultDto
   ├─ ReadResultDto(+ byte[] Data)
   └─ WriteResultDto
record HealthResultDto(bool Ok, string HostVersion, int RouteCount)                  // 不派生，Offline()
record RouteDto / RouteStatusDto / ProtocolDescriptorDto(Supports(kind), DefaultTransport) / SerialPortDto(Display)
```
**UI 禁止再定义同形状结果类型**；WebMaster 的 `DeviceOperationResult` 是 `HostingOperationResult` 的派生（加 `FailureKind`）。

### 7.3 `RegisterFailure` / `RegisterFailureKind`

- `Classify(string errorCode)`：`TransportIoError / Timeout / TransportUnavailable / RPC_ERROR / TIMEOUT` → `Unreachable`；`ProtocolNotFound / InvalidArgument / RouteConflict` 及未知 → `BadConfiguration`。
- `ShouldKeepConfiguration(code)` = `Classify(code) == Unreachable`。UI「添加设备」失败时据此决定**保留还是删除本地配置**。
- 与引擎 `ShouldAttemptReconnect` 的判断保持一致，由 `Tests/RegisterFailureClassifyTests` 锁定。

### 7.4 `ValueCodec`（静态）

`DefaultLength(dataType)`、`IsVariableLength(dataType)`、`ParseOrder(string)`、`Decode(byte[], dataType, ByteOrder)`、`Encode(string, dataType, ByteOrder)`。
`ByteOrder { ABCD, CDAB, BADC, DCBA }`。**UI 层禁止直接用 `BitConverter`**，一律经此并显式传设备配置的 `ByteOrder`。

### 7.5 `JsonFileStore`（静态）

`Load<T>(path, out error)`、`Save<T>(path, List<T>, out error)`、`SaveObject<T>(path, T, out error)` — 临时文件 + `File.Replace` 原子写。**所有本地配置落盘必须经此**。

---

## 8. WPF 客户端 `UI.Wpf`

### 8.1 `App.OnStartup` 顺序（`App.xaml.cs`）

```
 1. InstallGlobalExceptionHandlers()                    // UI 线程 / TaskScheduler / AppDomain
 2. _host = Host.CreateDefaultBuilder()
        .UseContentRoot(AppContext.BaseDirectory)
        .ConfigureServices(ConfigureServices)           // 见 8.2
        .ConfigureLogging(...)
        .Build()
 3. await _host.StartAsync()
 4. _ = GetRequiredService<DeviceListModel>()           // ★ 必须先于 Load()，见下
    GetRequiredService<IDeviceService>().Load()         // 拉服务端路由表与本地 devices.json 合并
 5. GetRequiredService<VariablePollingService>().Start()
 6. GetRequiredService<HostingSessionService>().Start() // 每 10s HealthAsync
 7. GetRequiredService<MainWindow>().Show()
```

> **第 4 步的顺序不能反。** `Load()` 是即发即忘的：后台拿到路由后**发一次 `DevicesChanged` 就结束，不缓存结果**。
> `DeviceListModel` 是这个事件的唯一订阅者，而它本来要等第 7 步建主窗口时才被 DI 顺带解析——
> 那时首次对账的事件早已发完。表现是界面上只剩本地配置、全部显示离线，要操作员手动点一次「刷新」才对。
> 这类 bug 编译与单元测试都发现不了。

### 8.2 `ConfigureServices` 注册清单（全部显式 lambda 构造注入）

| 注册 | 生命周期 | 构造 |
|---|---|---|
| `HostingClient` | Singleton | `new HostingClient(WpfAppSettings.ReadAddress(config), logger)` — 地址优先级 `config/settings.json` → `appsettings.json` → `http://localhost:5000` |
| `IAppLogger` → `MemoryAppLogger` | Singleton | 无参，环形 500 条，`EntryAdded` 事件在**后台线程**触发 |
| `IDeviceService` → `GrpcDeviceService` | Singleton | `new GrpcDeviceService(HostingClient, IAppLogger)` — **不含任何 WPF 类型** |
| `IRouteReconciler` | Singleton | `(IRouteReconciler)sp.GetRequiredService<IDeviceService>()` — **同一实例** |
| `DeviceListModel` | Singleton | `new DeviceListModel(IDeviceService)` — 订阅设备事件并切回 UI 线程；**必须是单例**，三个页面共享同一份列表，各建一份会让同一台设备在不同页面显示成不同状态 |
| `IVariableService` → `LocalVariableStore` | Singleton | `new LocalVariableStore(HostingClient)` |
| `IProtocolResolver` → `GrpcProtocolResolver` | Singleton | `new GrpcProtocolResolver(HostingClient)`；`RefreshAsync` copy-on-write，`SourceState` = Real / OfflineCache / Unavailable |
| `ISerialPortProvider` → `GrpcSerialPortProvider` | Singleton | 直接代理 `QuerySerialPortsAsync` |
| `HostingSessionService` | Singleton | `new HostingSessionService(HostingClient, IAppLogger)` |
| `DevicePageViewModel` | Singleton | `(IDeviceService, DeviceListModel, IAppLogger)` — 增删连断走 `IDeviceService`，列表与失败提示走 `DeviceListModel` |
| `LogPageViewModel` | Singleton | `(IAppLogger)` — 订阅 `EntryAdded` |
| `VariablePageViewModel` | Singleton | `(IVariableService, DeviceListModel, IAppLogger)` — 本页只读设备列表，**不注入 `IDeviceService`** |
| `DataMonitorViewModel` | Singleton | `(IDeviceService, DeviceListModel)` — 订阅 `DeviceListModel.Devices.CollectionChanged`；`IDeviceService` 仅用于「刷新」命令 |
| `SettingsViewModel` | Singleton | `(IConfiguration)` — 测试连接时**现场建临时 `HostingClient`**，保存经 `JsonFileStore.SaveObject` |
| `DataMonitorPage / DevicePage / VariableConfigPage / LogPage / SettingsPage` | **Transient** | 各注入对应 ViewModel；`DevicePage` 额外注入 `IProtocolResolver, ISerialPortProvider` |
| `VariablePollingService` | Singleton | `new VariablePollingService(IVariableService, HostingClient, IRouteReconciler)` |
| `MainWindow` | Singleton | `MainWindow(IServiceProvider, HostingSessionService)` — 这是**唯一允许持有 `IServiceProvider`** 的地方（按 `Type` 导航） |

### 8.3 导航

`NavSidebar.NavigateRequested(Type)` ← RadioButton `Tag` 里的 Page 类型
→ `MainWindow.NavigateTo(Type)` → `services.GetRequiredService(pageType)`（Transient 新建）→ `MainFrame.Navigate(page)`。
`MainWindow` 同时订阅 `session.Changed` 刷新顶栏在线灯/版本/路由数。

### 8.4 `GrpcDeviceService : IDeviceService, IRouteReconciler`

```csharp
public GrpcDeviceService(HostingClient client, IAppLogger? log = null)
  // 内部 new DeviceConfigStore(log)（构造即 Load config/devices.json）
  // _reconcileGate = new RouteReconcileGate(TimeSpan.FromSeconds(5))
```

**本类不引用 `System.Windows`，不持有任何界面集合，不做任何线程切换。**
它只产出数据并发事件；落到 `ObservableCollection` 是 `DeviceListModel`（§8.5）的职责。
所有事件**都可能在后台线程触发**。

| 事件 | 载体 | 何时发 |
|---|---|---|
| `DevicesChanged` | `IReadOnlyList<DeviceListItem>` | `Load()` 与宿主对账完成，整表快照 |
| `DeviceUpserted` | `DeviceListItem` | 单台加入/更新（配置已存但宿主注册失败的离线占位） |
| `DeviceRemoved` | `string routeId` | 宿主与本地都已删除 |
| `DeviceStatusChanged` | `DeviceStatusUpdate` | `WatchRouteStatus` 流推来的连接状态 |
| `OperationFailed` | `string` | 即发即忘操作失败的操作员可读描述 |

`DeviceListItem { DeviceInfo Device; bool PresentOnHost; }` — `Device` 是**新构造实例**，尚未被绑定引用，可安全跨线程传递；
`PresentOnHost=false` 表示"本地有配置、宿主没有"（宿主重启丢了内存路由），订阅方据此把它显示为离线并等对账补注册。

| 方法 | 内部调用 |
|---|---|
| `Snapshot()` | 由 `_config.GetAll()` 直接构造，全部 `PresentOnHost=false`。供订阅方在订阅前做初始填充——宿主没起来时 `Load()` 会失败且不发事件，没有这一步界面就是空的 |
| `Load()` | 后台 `QueryRoutesAsync()` → `BuildSnapshot(routes)` 合并 → `DevicesChanged`。宿主不可达时**只记日志、不发事件**，保留界面现有列表 |
| `BuildSnapshot(routes)`（私有） | 结果 = 宿主侧路由 ∪ 本地配置。两边都没有的条目自然不在结果里，订阅方据此移除 |
| `Add(DeviceInfo)` | `_config.Save(info)` → 后台 `RegisterAsync(info)` → `RegisterRouteAsync(…, info.MinIoIntervalMs)`；失败按 `RegisterFailure.ShouldKeepConfiguration` 决定是否 `_config.Delete`，保留则发 `DeviceUpserted` 离线占位 |
| `Update(DeviceInfo)` | `_config.Save` → `RemoveRouteAsync` 成功后 → `RegisterAsync` |
| `Remove(id)` | `RemoveRouteAsync` → **先** `_config.Delete(id)` **再** 发 `DeviceRemoved`（顺序不能反：订阅方收到事件后可能立刻取快照，配置还在的话删掉的设备会当场复活） |
| `ConnectAsync(id, ct)` | 取消旧 watch CTS → 新建 linked CTS → 发一条 `Connecting` → 后台 `WatchRouteStatusAsync`，每帧转成 `DeviceStatusChanged`。**同步返回 `Task.CompletedTask`**，不等待任何 I/O |
| `Disconnect(id)` | 取消该路由 watch CTS → 发一条 `Offline` |
| `EnsureRouteAsync(routeId, ct)`（`IRouteReconciler`） | 查本地记录 → `_reconcileGate` 合并 5s 内并发请求 → `ReconcileCoreAsync`：用 `DeviceRecord` 全部字段（含 `MinIoIntervalMs`）重新 `RegisterRouteAsync(…, ct)` |

> `DeviceRecord → DeviceInfo` 的映射有**三个入口**：`ToDeviceInfo`、`BuildSnapshot`、以及 WebMaster 侧的对应实现。
> 新增字段必须三处同步——漏一处的表现是"改了能保存、下次打开又变回默认值"，且不报任何错。
> `MinIoIntervalMs` 就在这三处各漏过一次。

### 8.5 `DeviceListModel`（`ViewModels/DeviceListModel.cs`）

**全项目唯一为设备列表调用 `Dispatcher` 的地方。** 纪律 4：服务只发布事件，切线程是订阅方的责任。

```csharp
public DeviceListModel(IDeviceService service)
  // 1) 先用 service.Snapshot() 填充 Devices（构造发生在 UI 线程，可直接写）
  // 2) 再订阅五个事件
public ObservableCollection<DeviceInfo> Devices { get; }   // 只允许 UI 线程读写
public event Action<string> OperationFailed;               // 已切到 UI 线程后转发
```

- 私有 `OnUi(Action)`：`Application.Current == null` → 丢弃（界面都没了）；`Dispatcher.CheckAccess()` 为真 → **就地执行**，否则 `InvokeAsync`。
  就地执行这一支是必要的：一律 `InvokeAsync` 会把修改推迟到下一轮消息循环，调用方紧接着读集合就会读到旧值。
- `DevicesChanged` → 按 Id **增量比对**，不整表替换：整体替换会打断 `DataGrid` 的选中项与滚动位置。
- 已存在的条目**拷字段而非替换实例**：替换会让指向旧实例的绑定全部失效。
- **连接状态只在 `PresentOnHost == false` 时才由列表刷新改写**。宿主侧仍有路由时真实状态由 `WatchRouteStatus` 推送，
  列表刷新越权改写会让一台正常通讯的设备闪成离线，直到下一个状态事件才恢复。

> 三个 ViewModel 与三个变量页子控件（`VariableDeviceList / VariableDeviceHeader / VariableImportPanel`）
> 都从这里取集合，**不再持有 `IDeviceService`**。

### 8.6 `DeviceConfigStore`
构造 `(IAppLogger? log = null)` → `Load()`；文件 `WpfPaths.DevicesFile = {BaseDirectory}/config/devices.json`；`GetAll / Get / Save(DeviceInfo) / Delete / FlushToDisk`，内部 `lock`，落盘经 `JsonFileStore`。
`DeviceRecord` 字段：`Id, Protocol, TransportKind, Ip, Port, Station, StationNo, SerialPort, BaudRate, MinIoIntervalMs, Name, Model, IsDualLane, ExtraSettingsJson`。

### 8.7 `VariablePollingService`
构造 `(IVariableService, HostingClient, IRouteReconciler?)`。
`Start()`：订阅 `VariablesChanged`，首次 `OnVariablesChanged()` 差量同步 `PollHandle` 集合。
每个 `IsPollingEnabled` 的变量一个后台循环：`Task.Delay(ScanRateMs)` → `client.ReadAsync(routeId, address, length)` → 成功则 `ValueCodec.Decode(data, type, 设备 ByteOrder)`；遇 `RouteNotFound` → `reconciler.EnsureRouteAsync(routeId)`。
`Dispose()`：取消全部句柄（`App.OnExit` 第一步调用）。

> **仍有 3 处 `Application.Current.Dispatcher`**（写 `VariableItem.LastValue / LastError`）。
> 与 §8.4 已清理的那批性质不同：那里改的是 `ObservableCollection`，不切线程会抛 `NotSupportedException`；
> 这里改的是绑定对象的**标量属性**，WPF 绑定层本就会自动 marshal `PropertyChanged`，因此是分层问题而非故障。
> 按纪律 4 应同样改为发事件 + UI 侧订阅，改法与 §8.5 同构。**这是当前唯一待清理的结构性问题。**

### 8.8 `HostingSessionService`
构造 `(IHostingClient, IAppLogger?)`。`Start()` 启动 `PollLoopAsync`：每 10s `HealthAsync`，只在 `Online / HostVersion / RouteCount` 变化时触发 `Changed`。

### 8.9 `LocalVariableStore : IVariableService`
构造 `(HostingClient)`。内存表 + `config/variables.json`；`Add / Update / Remove` 触发 `VariablesChanged`（任意线程）；`WriteAsync(id, value, ct)` = `ValueCodec.Encode` → `client.WriteAsync`。

### 8.10 `App.OnExit`
```
 VariablePollingService.Dispose()                         // 先停所有 ReadAsync 循环
 await _host.StopAsync(5s)
 await ((IAsyncDisposable)_host).DisposeAsync()           // HostingClient 只实现 IAsyncDisposable
```

---

## 9. Web 客户端 `UI.WebMaster`

### 9.1 `Main.cs` 启动顺序

```
 1. builder = WebApplication.CreateBuilder(args)
 2. listenPort = WebSettingsStore.ResolveListenPort(config)  (缺省 80)
    grpcPort   = config["Hosting:GrpcPort"] ?? 5000
 3. builder.WebHost.ConfigureKestrel(o => {
        o.ListenAnyIP(listenPort, Http1AndHttp2);            // Blazor
        o.ListenAnyIP(grpcPort, HostingComposition.ListenGrpc); // 内嵌 gRPC，HTTP/2 only
    })
 4. 服务注册（顺序）：
    a. logStore = new AppLogStore(); AddSingleton(logStore); Logging.AddProvider(new AppLogLoggerProvider(logStore))
    b. AddRazorComponents().AddInteractiveServerComponents(); Configure<CircuitOptions>(保留 3 分钟)
    c. AddSingleton<WebSettingsStore / WebProxySettingsStore / WebAuthStore / WebTunnelSettingsStore>()
    d. AddSingleton(new WebRuntimeInfo(listenPort, grpcPort))
    e. AddSingleton<FrpcHost>(); AddHostedService(sp => sp.GetRequiredService<FrpcHost>())   // 内网穿透，可选
    f. Cookie 认证（1 天滑动过期，由 WebAuthStore 控制）
    g. ForwardedHeaders（仅 X-Forwarded-Proto / Host）、HSTS 30 天
    h. AddSingleton<WebDeviceStore / WebVariableStore / WebTemplateStore>()
    i. HostingComposition.AddServices(builder.Services, config)               ← 引擎进同一进程
       AddSingleton<IHostingClient>(sp => new HostingClient("http://127.0.0.1:" + grpcPort, logger))
    j. AddSingleton<EngineSession>(); AddHostedService(sp => sp.GetRequiredService<EngineSession>())
       AddSingleton<IWebDeviceService, WebDeviceService>()
       AddSingleton<IWebVariableService, WebVariableService>()
       AddHostedService<VariablePoller>()
       AddHostedService<TrayHost>()                                           // 托盘图标
 5. app = builder.Build()
 6. HostingComposition.Warmup(app)          ← EngineRuntime 构造、插件扫描、链路循环启动
 7. HostingComposition.MapEndpoints(app)    ← gRPC 端点
 8. 中间件：ForwardedHeaders → HTTPS 重定向 → 静态文件 → 认证/授权 → WebAuthEndpoints → MapRazorComponents<App>()
 9. app.Run()                               // 启动失败写 startup-error.log 后退出
```

**同进程内的 gRPC 环回**：`EngineSession.Client` 打 `127.0.0.1:{grpcPort}`，与 WPF 走同一套 `HostingClient` 代码。拆开部署时只改客户端目标地址，不做「切换远端 Host」功能。

### 9.2 HostedService 及其循环

#### `EngineSession : IHostedService, IAsyncDisposable`
```csharp
public EngineSession(IHostingClient client, ILogger<EngineSession> logger, WebDeviceStore devices, AppLogStore log, IConfiguration config)
```
`StartAsync(ct)`：替换 `_loopCts`，`Task.Run(HealthLoopAsync)` + `Task.Run(WatchLoopAsync)`，**立即返回**。

| 循环 | 节拍 | 做什么 |
|---|---|---|
| `HealthLoopAsync` | 5s | `client.HealthAsync` → 更新 `Online / HostVersion / RouteCount / LastError`；`QueryRoutesAsync` 更新 `Routes`；变化时 `Changed` 事件 |
| `WatchLoopAsync` | 长连接 | `client.WatchRouteStatusAsync("", onStatus)` 订阅全部路由；写 `_status: ConcurrentDictionary<string, RouteStatusDto>`；断流 → `MarkAllOffline()` → 指数退避重连（1s → 30s） |

公开：`Client`、`Address`、`Online`、`HostVersion`、`RouteCount`、`LastError`、`Routes`、`StatusSnapshot()`、`IsRouteOnline(id)`、`GetStatus(id)`、`event Action? Changed`。
**Razor 页订阅 `Changed` 后必须 `InvokeAsync(StateHasChanged)`** —— 服务层不切线程。

#### `VariablePoller : IHostedService`
```csharp
public VariablePoller(EngineSession session, WebVariableStore store, IWebVariableService variables, ILogger<VariablePoller> logger)
```
`StartAsync` → `Task.Run(LoopAsync)`。`LoopAsync` 固定 200ms 节拍：`_session.Online` 为真才 `TickAsync`：
```
 清理已删变量的 _nextDue
 foreach v in store.GetAll()
   跳过：未勾选 Polling / 无 RouteId / 无 Address / 未到 ScanRateMs 到期
   路由离线 → store.ApplyRead(v.Id, "OFFLINE", isError: true)
   否则 → outcome = await variables.ReadAsync(v) → store.ApplyRead(...)
```

#### `FrpcHost`、`TrayHost`
与通讯链路无关：前者管理 frpc 子进程（内网穿透），后者 WinForms 托盘图标 + 日志窗口。

### 9.3 服务与存储

| 类 | 构造 | 关键方法 |
|---|---|---|
| `WebDeviceService : IWebDeviceService` | `(EngineSession session, WebDeviceStore store, AppLogStore log)` | `ConnectAsync(routeId)` → 读 `store.Get` → `session.Client.RegisterRouteAsync(全部字段含 MinIoIntervalMs)` → 失败封装 `DeviceOperationResult.Failed`（自动 `RegisterFailure.Classify`）；`DisconnectAsync` / `UnregisterForDeleteAsync` → `RemoveRouteAsync`（后者容忍 RouteNotFound）；`QueryProtocolsAsync` / `QuerySerialPortsAsync` 直通 |
| `WebVariableService : IWebVariableService` | `(EngineSession session, WebDeviceStore devices)` | `ReadAsync(WebVariable)` → `Client.ReadAsync` → `ValueCodec.Decode(data, type, 设备记录的 ByteOrder)` → `VariableReadOutcome`；`WriteAsync(WebVariable)` → `ValueCodec.Encode` 失败直接 `PARSE_ERROR` 不发 I/O，否则 `Client.WriteAsync` |
| `WebDeviceStore` | `(AppLogStore log)` → `Load()` | `GetAll / Get / Upsert / Delete`，`lock`，`JsonFileStore`；`WebDeviceRecord` 含 `RouteId, Name, ProtocolId, TransportKind, Address, Port, Station, SerialPort, BaudRate, MinIoIntervalMs, ByteOrder` |
| `WebVariableStore` | `(AppLogStore log)` | 变量 CRUD、`ApplyRead(id, display, isError)`、`ImportJson / ExportJson`、`ApplyAddressChanges`（批量改地址）、模板同步 |
| `WebTemplateStore` | `(AppLogStore log)` | 设备模板（`WebDeviceTemplate` / `WebDeviceTemplateSlot`） |
| `AppLogStore` + `AppLogLoggerProvider` | 无参 / `(AppLogStore)` | 把 `ILogger` 输出汇入内存环形缓冲供 LogPage 与托盘窗口显示 |
| `WebSettingsStore / WebProxySettingsStore / WebAuthStore / WebTunnelSettingsStore` | DI 无参 | 各自 JSON 配置，经 `JsonFileStore` |

### 9.4 Razor 页面注入
- 所有页：`EngineSession`（在线状态、路由表、`Changed` 事件）
- `DevicesPage`：`IWebDeviceService`、`WebDeviceStore`、`WebTemplateStore`
- `VariablesPage`：`IWebVariableService`、`WebVariableStore`、`WebTemplateStore`、`AppLogStore`
- `LogPage`：`AppLogStore`
- 页面**不得**直接持有 `IHostingClient`。

---

## 10. 端到端时序

### 10.1 进程启动（WebMaster 内嵌模式）

```
Main
 ├─ Kestrel 监听 :80 (Blazor) 与 :5000 (gRPC/H2)
 ├─ DI 注册（§9.1 步骤 4）
 ├─ builder.Build()
 ├─ HostingComposition.Warmup
 │    └─ GetRequiredService<EngineRuntime>
 │         ├─ 解析 IRouteAssemblyService → new PluginRouteAssemblyService
 │         │     └─ LoadFactories → PluginCatalog.DiscoverAndLoad(plugins/) → 实例化所有 Factory
 │         ├─ 解析 IRouterOrchestrator → new RouterOrchestrator(new ConnectionRouter, new ReadCoordinator)
 │         └─ EngineRuntime ctor → TryEnterProcess → 启动 LinkCheckLoopAsync(5000)
 ├─ MapEndpoints → HostingGrpcService 可用
 └─ app.Run()
      └─ IHostedService.StartAsync 依注册顺序：
           FrpcHost → EngineSession(HealthLoop + WatchLoop) → VariablePoller(Loop) → TrayHost
```

### 10.2 用户在设备页点「连接」

```
DevicesPage.OnConnect(routeId)
 └─ IWebDeviceService.ConnectAsync(routeId)
      └─ WebDeviceStore.Get(routeId) → WebDeviceRecord
      └─ EngineSession.Client.RegisterRouteAsync(routeId, protocolId, kind, address, port, station, serialPort, baud, minIoIntervalMs, ct)
           ══ gRPC ══▶ HostingGrpcService.RegisterRoute
                        └─ EngineRuntime.RegisterRouteAsync(cmd)
                             ├─ 占位
                             ├─ PluginRouteAssemblyService.AssembleAsync
                             │    └─ RouteAssembler.AssembleAsync
                             │         ├─ 选协议工厂 / 传输工厂
                             │         ├─ TcpTransportFactory.CreateClient() → TcpTransportClient
                             │         ├─ await client.ConnectAsync(endpoint)      ← 真正建 TCP
                             │         ├─ ModbusTcpProtocolDriverFactory.CreateDriver(ctx{Station})
                             │         └─ new RouteEntry {...}
                             ├─ _routeIdByKey.TryAdd / RouterOrchestrator.TryRegister → ConnectionRouter.TryAdd
                             ├─ _registrationsByRouteId[...] = ...
                             └─ PublishStatus(online:true) → RouteStatusChanged
                                   └─ HostingGrpcService.WatchRouteStatus 的 OnStatus → channel → 服务器流
           ◀══ RegisterRouteResultDto
      └─ 失败：DeviceOperationResult.Failed(code) → FailureKind = RegisterFailure.Classify(code)
              UI 据 Unreachable / BadConfiguration 决定保留/删除本地记录

EngineSession.WatchLoopAsync 收到 RouteStatusDto(Online=true)
 └─ _status[routeId] = dto → Changed → 页面 InvokeAsync(StateHasChanged) → 状态灯变绿
```

### 10.3 变量轮询读一次（Modbus TCP 举例）

```
VariablePoller.TickAsync（200ms 节拍，变量到期）
 └─ WebVariableService.ReadAsync(row)
      └─ Client.ReadAsync(routeId, "40001", 2)
           ══ gRPC ══▶ HostingGrpcService.Read
                        └─ EngineRuntime.ReadByRouteIdAsync(routeId, "40001", 2)
                             └─ RouterOrchestrator.ExecuteReadAsync(key=(RouteKey,"40001",2), action)
                                  └─ ReadCoordinator.ExecuteAsync            // 同键并发读只发一次
                                       └─ action(ct) = EngineRuntime.ExecuteWithRoutePolicyAsync
                                            └─ RouteEntry.ExecuteExclusiveAsync   // 拿锁；串口补帧间静默
                                                 └─ RunReadWithPolicyAsync
                                                      └─ ModbusProtocolDriver.ReadAsync(transport, "40001", 2)
                                                           ├─ ModbusAddress.Parse("40001") → HoldingRegister #0
                                                           ├─ 2 字节 → 1 寄存器 → BuildReadFrame → MBAP + FC03
                                                           └─ TcpTransportClient.SendAndReceiveAsync(frame, TryGetFrameLength)
                                                                ├─ DiscardResidual / DrainSocketBuffer
                                                                ├─ stream.WriteAsync
                                                                └─ FrameReader.ReadFrameAsync → 协议回调判帧长
                                                           └─ Unwrap → 校验事务 Id / 异常码 → Ok(byte[2] 大端)
                                                      └─ PublishStatus(online:true)
           ◀══ ReadResultDto(Data)
      └─ ValueCodec.Decode(data, "Int16", 设备 ByteOrder) → VariableReadOutcome
 └─ WebVariableStore.ApplyRead(id, display, isError:false)
```

### 10.4 掉线与自愈

```
拔网线
 ├─ 路径 A（有轮询）：ReadAsync 返回 TransportIoError
 │     RunReadWithPolicyAsync → ShouldAttemptReconnect → TryReconnectAsync（Disconnect→Connect 原 Endpoint）
 │        成功 → 同锁内重试一次；失败 → PublishStatus(offline)
 └─ 路径 B（无轮询）：LinkCheckLoopAsync 5s 一轮
       IsConnectionAlive=false → RecoverDeadLinkAsync；或空闲 5s → ProbeIdleRouteAsync(driver.ProbeAsync)

PublishStatus(offline) → RouteStatusChanged → WatchRouteStatus 流 → EngineSession._status → 页面灯变红
WPF 侧：GrpcDeviceService.ConnectAsync 里的 WatchRouteStatusAsync 回调 → Dispatcher → DeviceInfo.IsConnected=false

Host 进程自身掉了：
 ├─ Web：EngineSession.WatchLoop 断流 → MarkAllOffline → 退避重连；HealthLoop Online=false；VariablePoller 整批跳过
 └─ WPF：HostingSessionService 10s Health 失败 → Changed；VariablePollingService 读到 RouteNotFound → IRouteReconciler.EnsureRouteAsync 重新注册
```

---

## 11. 后台循环一览表

| 所在 | 类.方法 | 启动时机 | 节拍 | 停止 |
|---|---|---|---|---|
| 引擎 | `EngineRuntime.LinkCheckLoopAsync` | `EngineRuntime` 构造函数 | 5s（`linkCheckIntervalMs`，≤0 关闭） | `EngineRuntime.DisposeAsync` |
| 引擎 | `ReadCoordinator.InflightRead.RunAsync` | 首个参与者 `Join` | 一次性 | 参与者归零 / 完成 |
| Hosting | `HostingGrpcService.WatchRouteStatus` 流循环 | 客户端发起 RPC | 事件驱动 | 客户端取消 / 断连 |
| Web | `EngineSession.HealthLoopAsync` | `IHostedService.StartAsync` | 5s | `StopAsync` |
| Web | `EngineSession.WatchLoopAsync` | 同上 | 长连接 + 1s→30s 退避 | `StopAsync` |
| Web | `VariablePoller.LoopAsync` | `StartAsync` | 200ms 节拍，按变量 `ScanRateMs` 到期 | `StopAsync` |
| Web | `FrpcHost` | `StartAsync` | 子进程监护 | `StopAsync` |
| WPF | `HostingSessionService.PollLoopAsync` | `App.OnStartup` 第 6 步 `Start()` | 10s | `Dispose` |
| WPF | `VariablePollingService` 每变量循环 | `App.OnStartup` 第 5 步 `Start()`；`VariablesChanged` 差量 | 各自 `ScanRateMs` | `App.OnExit` 首步 `Dispose` |
| WPF | `GrpcDeviceService.ConnectAsync` 内 Watch | 用户点连接 | 事件驱动 | `Disconnect(id)` / 再次 Connect |
| WPF | `GrpcProtocolResolver.RefreshAsync` | 设备页打开 | 一次性 | — |

---

## 12. 生命周期与释放顺序

### Hosting.App 独立进程
```
Ctrl+C / SIGTERM
 → IHost.StopAsync → gRPC 停收新请求
 → ServiceProvider.DisposeAsync
     └─ EngineRuntime.DisposeAsync
          ├─ _linkCheckCts.Cancel → await _linkCheckTask
          ├─ foreach routeId: UnregisterRouteAsync → RouterOrchestrator.TryRemoveAndDisposeAsync
          │     └─ RouteEntry.DisposeAsync → TransportClient.DisposeAsync（关 socket / 串口）
          └─ LeaveProcess()（释放进程互斥）
```

### WebMaster
同上，外加 `IHostedService.StopAsync` 逆注册顺序：`TrayHost → VariablePoller → EngineSession → FrpcHost`。`EngineSession.DisposeAsync` 释放 `_loopCts`；`IHostingClient` 单例随容器 `DisposeAsync`。

### WPF
`App.OnExit`（§8.10）。注意 `HostingClient` 只实现 `IAsyncDisposable`，容器必须走 `DisposeAsync`，否则退出时抛 `InvalidOperationException`。

---

## 附：阅读源码的建议顺序

1. `Core.Abstractions/Results/*` + `Errors/KernelErrorCode.cs` — 所有返回值的语言
2. `Core.Transport/Abstractions/ITransportClient.cs` + `Framing/FrameReader.cs` — 传输如何不认识协议
3. `Core.Protocol/Abstractions/IProtocolDriver.cs` → 任选一个插件的 `Driver.ReadAsync` — 协议解析边界
4. `Core.EngineRouter/Runtime/RouteEntry.cs` — 一把锁如何保护一条物理链路
5. `Core.EngineRuntime/RouteAssembler.cs` → `EngineRuntime.RegisterRouteAsync` → `ReadByRouteIdAsync` → `LinkCheckLoopAsync`
6. `Hosting.App/HostingComposition.cs` → `HostingGrpcService.cs`
7. `Hosting.Sdk/HostingClient.cs`
8. `UI.WebMaster/Main.cs` → `EngineSession.cs` → `VariablePoller.cs`
9. `UI.Wpf/App.xaml.cs` → `GrpcDeviceService.cs` → `ViewModels/DeviceListModel.cs` — 服务发事件、订阅方切线程的分界线在哪
10. `UI.Wpf/Services/VariablePollingService.cs`
