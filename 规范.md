# CommunicationKernel — 开发规范

新增代码前读第一节。怎么跑见 [`README.md`](README.md)；逐类逐方法的实现细节见 [`架构.md`](架构.md)；现场发布见 [`部署-Linux与树莓派.md`](部署-Linux与树莓派.md)。

本文档回答的是**「为什么必须这样写」**，不是「有什么类」。每条规范背后都有一次实际发生过的事故，第七节按事故形态归档。

> 本文件曾名 `计划.md`。改名是因为里面从来没有排期，全是约束——叫「计划」会让人点进来找进度表。
> 历史记录用 `git log --follow 规范.md` 可以完整追溯。

---

## 目录

1. [金字塔（不可打破）](#一金字塔不可打破)
2. [数据流](#二数据流)
3. [Router](#三router)
4. [结果类型](#四结果类型)
5. [协议地址](#五协议地址)
6. [工程树](#六工程树16)
7. [事故档案：不要再犯](#七事故档案不要再犯)
8. [容易写错的语义](#八容易写错的语义)
9. [仓库级约定](#九仓库级约定)

---

## 一、金字塔（不可打破）

```
L7  UI.Wpf / UI.WebMaster
    只持有 route_id 与 SDK DTO。页面不持有传输客户端。
        │  Hosting.Sdk（gRPC；WebMaster 打本机回环）
L6  Hosting.App  +  Hosting.Sdk
    App：组合根 + gRPC 翻译，不做业务。
    Sdk：零工程引用。含 ValueCodec / JsonFileStore / RegisterFailure。
        │
L5  Core.EngineRuntime     路由生命周期 / 巡检 / 单次重连 / 状态事件
L4  Core.EngineRouter      路由表 + 同键读合并；独占门在 RouteEntry
L3  Plugin.Context         ALC 加载，只认 Core.Abstractions
L2  Plugins.Protocol.*     协议知识的唯一合法位置
L1  Core.Transport + 传输插件    字节，不解释
L0  Core.Abstractions      契约根，零工程引用
```

| 层 | 允许知道 | 严禁知道 |
|---|---|---|
| UI | route_id、视图、SDK DTO | 帧、地址语义、协议字段 |
| HostingGrpcService | gRPC DTO、Runtime 公开方法 | Runtime 内部表 |
| EngineRuntime | route_id、RouteKey、运行策略 | 帧内容、地址解析 |
| Router | RouteKey、RouteEntry（不透明） | 协议语义 |
| Transport | 字节流、连接 | 编解码 |
| 协议插件 | 协议全部语义 | 业务 / UI |

依赖只向下。唯一反向流：`EngineRuntime.RouteStatusChanged`（下层发布、上层订阅）。

### 结构纪律

> 状态列说明：**强制**＝当前代码已完全符合；**收尾中**＝主体已符合，尚有明确的遗留点。

| # | 纪律 | 状态 |
|---|---|---|
| 1 | **工程引用必须有实际 `using`。** 禁止以防万一 | 强制 |
| 2 | **`*.Abstractions` 只放接口、委托、枚举、不可变契约。** 有状态实现放别的子命名空间 | 强制 |
| 3 | **UI 页面不得直接持有传输客户端。** WPF：`IDeviceService` / `IVariableService`；Web：`IWebDeviceService` / `IWebVariableService` | 强制 |
| 4 | **服务只发事件，不认识视图。** 切回 UI 线程是订阅方的事，服务里禁止 `Dispatcher` | 收尾中，见下 |
| 5 | **构造注入优先。** `IServiceProvider` 只留给运行时才知道的类型（按 `Type` 导航）；组合根除外 | 强制 |
| 6 | **本地配置一律 `Hosting.Sdk.JsonFileStore`。** 禁止 `File.WriteAllText` | 强制 |

**纪律 2 的判据不是"叫什么名"，是"有没有状态"。** `RouteEntry` 持有 `SemaphoreSlim`、记录上次 I/O 时刻、实现 `IAsyncDisposable`——它是实现，因此在 `EngineRouter/Runtime/` 而非 `Abstractions/`。`FrameReader` 同理，在 `Core.Transport/Framing/`。
命名空间约定：`Abstractions/` → `.Abstractions`；`Models/` → `.Models`；`Runtime/` → **根命名空间，不加后缀**。

**纪律 4 的当前状态**：

| 位置 | 状态 |
|---|---|
| `UI.Wpf/Services/GrpcDeviceService.cs` | ✅ 已清理。改为发 5 类事件（`DevicesChanged` / `DeviceUpserted` / `DeviceRemoved` / `DeviceStatusChanged` / `OperationFailed`），不引用 `System.Windows` |
| `UI.Wpf/ViewModels/DeviceListModel.cs` | ✅ 合规。它是**订阅方**，用 `Dispatcher` 正是纪律要求的分工 |
| `UI.Wpf/Services/VariablePollingService.cs` | ⚠️ 3 处待清理（写 `VariableItem.LastValue / LastError`） |

遗留那 3 处与已清理的那批**性质不同**：已清理的改的是 `ObservableCollection`，不切线程会抛 `NotSupportedException`；遗留的改的是绑定对象的标量属性，WPF 绑定层本就会自动 marshal `PropertyChanged`，因此是分层问题而非运行故障。改法与 `DeviceListModel` 同构。

### 补充约定

- 对外失败形态只有 `HostingOperationResult` 派生系；禁止 UI 再造同形状类型；公共 API 禁止匿名 `ValueTuple`。
- 字节序走 `ValueCodec` 并传入设备 `ByteOrder`，禁止裸 `BitConverter`。本机小端，协议插件上抛大端。
- 两个 UI **不强行合并**（功能不对等）；只抽真正同源的 substrate。**但不允许其中一方违纪**——功能可以不对等，纪律必须对等。
- 改 Sdk / EngineRuntime 公共 API 必须更新 `ApiBaselines/`（见第九节）。
- UI 禁止硬编码协议表、串口表。协议来自 `QueryProtocols`，串口来自 `QuerySerialPorts`（**列的是宿主机器上的口**）。
- 串口三名：`SerialPortInfo`（引擎）/ `SerialPortDescriptor`（gRPC）/ `SerialPortDto`（SDK）。禁止第四个。
- Panasonic 只有 `panasonic-mewtocol`，TCP 与串口共用，**没有** `-tcp` / `-serial` 后缀。
- `RegisterRoute` 必 `ConnectAsync`；连不上不入表。UI「添加」只落盘。`RegisterFailureKind` 区分不可达与配置错。
- 读写互斥在 `RouteEntry.ExecuteExclusiveAsync`。不要再加 `WriteScheduler` / `SerialIoGate` / `SubscriptionHub`——这三件已从 Router 层删除。
- `length` 单位是**字节**。奇数长度不得静默向下取整。
- 站号只在设备配置；地址拒绝 `1:40001` 这类前缀。多站 = 多条路由。
- protobuf 只有 `Protos/V1/hosting.proto`。
- 本机一份引擎：WebMaster 与 `Hosting.App.exe` 互斥（互斥量保证）。
- 本文档是**规范**，不是待办清单。进度以代码为准，排期不在版本库里。

配置目录：各 exe 旁 `config/`。
Web：`web-listen.json` / `web-devices.json` / `web-variables.json` / `web-templates.json` / `web-auth.json` / `web-proxy.json` / `web-tunnel.json`。
WPF：`settings.json` / `devices.json` / `variables.json`。

---

## 二、数据流

**添加 ≠ 连接。** Web 添加只写本地 JSON，卡片「连接」才 `RegisterRoute`。WPF `Add` 会立刻注册，不可达仍留卡片（见第七节「添加失败后界面什么都没发生」）。

```
RegisterRouteAsync(cmd)
  校验 RouteId / 分隔符 / 重复
  AssembleAsync → 选工厂 + ConnectAsync + CreateDriver + 定 MinIoIntervalMs
  Orchestrator.TryRegister
  记三张表 + PublishStatus(online)

ReadByRouteIdAsync
  ReadCoordinator 同键合并
  RouteEntry.ExecuteExclusiveAsync（静默窗口 + 读 + 失败则单次重连）
  协议内部 BuildReadFrame / Parse；外层只见 byte[]

WriteByRouteIdAsync
  不合并；同一把独占门。仅传输层错误可重试。

UnregisterRouteAsync
  从表移除 + TryRemoveAndDisposeAsync（关 socket / 串口）
  顺序固定：先摘表再释放。反过来会让在途读写拿到已释放的连接。

WatchRouteStatus
  HostingGrpcService 订 RouteStatusChanged
  BoundedChannel(256, DropOldest)
  Web 全站一条流，EngineSession 持有
```

宿主重启后其内存路由全部消失，两个 UI 各有补注册路径：

| UI | 机制 | 触发时机 |
|---|---|---|
| Web | `EngineSession.ReconcileAsync` 按 `web-devices.json` 补注册 | 宿主从离线恢复 |
| WPF | `IRouteReconciler.EnsureRouteAsync` | 读写遇到 `RouteNotFound` 时 |

WPF 侧经 `RouteReconcileGate` 合并 5 秒内的并发请求——一台设备挂几十个变量时，宿主重启会让它们同时收到 `RouteNotFound`，逐个发起会打爆宿主。

---

## 三、Router

两件真职责：路由表、同键读合并。门控在 `RouteEntry`。

| 类型 | 文件 | 职责 |
|---|---|---|
| `ConnectionRouter` | `Runtime/ConnectionRouter.cs` | `RouteKey → RouteEntry`，`ConcurrentDictionary` |
| `ReadCoordinator` | `Runtime/ReadCoordinator.cs` | 同 `(RouteKey, address, length)` 合并一次 I/O |
| `RouteEntry` | `Runtime/RouteEntry.cs` | `SemaphoreSlim(1,1)` + `MinIoIntervalMs` |
| `RouterOrchestrator` | `Runtime/RouterOrchestrator.cs` | 以上三者的门面 |
| 三个接口 | `Abstractions/I*.cs` | **只有接口，没有实现** |

`RouteEntry._ioGate` **刻意不 Dispose**：在途 `Release` 会抛 `ObjectDisposedException`，而 `SemaphoreSlim` 不持有非托管资源，随 GC 回收即可。

---

## 四、结果类型

内核：`OperationResult` / `OperationResult<T>` + `KernelErrorCode`。
线上 / UI：`HostingOperationResult`（string 错误码），边界在 `HostingGrpcService`。

```
HostingOperationResult
  ReadResultDto / WriteResultDto
  RegisterRouteResultDto / RemoveRouteResultDto
  DeviceOperationResult + FailureKind   （WebMaster）
  VariableReadOutcome + DisplayValue    （WebMaster）
```

`HealthAsync` 单独：`HealthResultDto(Ok, HostVersion, RouteCount)`——不可达是探测结论，不是操作失败。

**错误码的可重连性必须全链路一致。** `EngineRuntime.ShouldAttemptReconnect`、`ProtocolProbe`、`RegisterFailure.Classify` 三处对同一个 `KernelErrorCode` 必须给出相同判断。分叉过一次，见第七节。

---

## 五、协议地址

站号写在设备上。地址里出现 `数字:` 前缀一律报错。多站 = 多条路由。

### Modbus（`modbus-tcp` / `modbus-rtu` / `modbus-ascii`）

| 地址 | 含义 |
|---|---|
| `40001` / `4x0001` | 保持寄存器 |
| `coil:5` / `00001` | 线圈 |
| `holding:` / `input:` / `discrete:` | 区内 0 基 |

`length=1` → FC01；`>1` → FC03。写：1 字节 FC05，2 字节 FC06，更长 FC10。RTU 附 CRC16；ASCII 为 `: … LRC CR LF`。

### Panasonic MEWTOCOL（`panasonic-mewtocol`，TCP 9094 或串口）

`DT100` / `R10A` / `R100` / `X5` / `Y3` / `WR50`。`length=1` 或位 → RCS，否则 RD（上限 128 字）。低字节先出。

**收发两侧都必须算 BCC，且计算范围含帧头 `%`。** 响应侧要在解释内容**之前**校验——包括帧自称是错误响应（`!42`）的情况：整帧不可信时里面的错误码同样不可信。`**` / `!!` 是规范允许的「本帧不校验」占位，须放行。

### Siemens S7（`siemens-s7-1200` / `siemens-s7-200smart`，102）

`DB10.DBW0` / `DBB` / `DBD` / `MB` / `IB` / `QB` / `VB`（200Smart→DB1）/ `T` / `C`。
握手：COTP CR→CC（至少 6 字节，`[5]==0xD0`）；S7 Setup `[8]=0x03,[17-18]=0,[19]=0xF0`。
TSAP：1200=`0x0300`，200Smart=`0x0200`。

---

## 六、工程树（16）

```
L0  Core.Abstractions
L1  Core.Transport          Abstractions + Framing/FrameReader
    Plugins.Transport.Tcp / SerialPort
L2  Core.Protocol           契约
    Plugins.Protocol.Modbus / Panasonic / Siemens.S7
L3  Plugin.Context
L4  Core.EngineRouter       Abstractions（纯接口）/ Models / Runtime
L5  Core.EngineRuntime      + PluginRouteAssemblyService / StaticRouteAssemblyService
L6  Hosting.App             HostingComposition + HostingGrpcService
    Hosting.Sdk
L7  UI.Wpf / UI.WebMaster
    Tests                   28 个测试文件
```

WebMaster：`HostingComposition` 把 App 装进同一进程；UI 仍走 Sdk（回环 gRPC），**不直连 `EngineRuntime`**——这样拆开部署时只需改客户端目标地址。
Hosting.Sdk 零工程引用。形态 A 第三方嵌入走 `StaticRouteAssemblyService`（测试也用它）。

`tools/SoakHarness/` 是压测工具，**刻意不在解决方案里**——它引用 `EngineRuntime` 直连，不经 gRPC，不属于产品代码。

---

## 七、事故档案：不要再犯

分四类。每条都实际发生过。

### 7.1 并发与时序

| 事故 | 症状 | 正确做法 |
|---|---|---|
| 读路径无互斥 | 串口上并发读写串数据；界面显示的值属于别的变量 | `RouteEntry` 一把锁，覆盖读+写 |
| **读被取消后连接失步** | 请求已发、响应未读时取消，响应随后抵达躺在内核缓冲；下次请求先读到它，从此错位一格 | `_desynced` 标记 + `DrainSocketBuffer()`。清缓冲只能丢**已到达**的字节，管不了**还在路上**的响应，两者缺一不可 |
| 本机两份 Runtime | 端口冲突或两套路由表互不可见 | 互斥量；WebMaster 已含宿主 |
| `TryRemove` 与 `DisposeAsync` 顺序反了 | 在途读写拿到已释放的连接 | 先摘表再释放 |

**失步那条的严重性取决于协议**：Modbus TCP 有事务 ID，能报出 `事务 ID 不匹配`，但 `ProtocolError` 不在重连判据里，这条路由再也无法自愈；**MEWTOCOL 没有事务 ID**，同样的错位不报任何错，只会让每个寄存器值都变成"上一次读到的那个"——帧完整、BCC 也对，校验和拦不住。由 `Tests/TcpCancelDesyncTests` 锁定。

### 7.2 数据正确性

| 事故 | 症状 | 正确做法 |
|---|---|---|
| 两套 UI 各写一遍落盘/解码，只有一遍对 | 同一设备在两个界面显示不同值 | `JsonFileStore` / `ValueCodec`；页面与轮询共用服务 |
| **MEWTOCOL 响应侧不校验 BCC** | RS-485 长线被噪声打翻一位的响应被当作有效寄存器值显示，还标着"成功" | 三个解析入口都先 `VerifyBcc`。由 `Tests/MewtocolResponseBccTests` 用**人为篡改的合成帧**锁定——不接真设备就没有噪声，自测覆盖不到 |
| **`MinIoIntervalMs` 不传，落到 SDK 默认 100** | WPF 注册的**每条链路（含 TCP）**被限死在 10 次 I/O／秒，界面无任何提示 | 显式传 `0`。见第八节 |
| **`DeviceRecord → DeviceInfo` 映射漏字段** | 改了能保存、下次打开又变回默认值，不报任何错 | 三个入口（`ToDeviceInfo` / `BuildSnapshot` / `Load` 回填）必须同步 |
| 非原子写配置 | 掉电留下截断 JSON，下次启动整份配置丢失 | 一律 `JsonFileStore`（临时文件 + `File.Replace`） |

### 7.3 分层与结构

| 事故 | 症状 | 正确做法 |
|---|---|---|
| `Abstractions` 里放 `FrameReader` / `RouteEntry` | 抽象目录里是有状态实现，判据失效 | 实现进 `Framing/` / `Runtime/` |
| 页面直接打 gRPC / 视图里做健康轮询 | 连接生命周期依附窗口，关窗即断 | 服务接口 + `HostingSessionService` |
| **服务层持有 `ObservableCollection` 并自己 `Dispatcher`** | 服务离开 WPF 无法实例化；`Application.Current` 在退出途中为 null 时更新与错误提示被**静默丢弃** | 服务发事件、`DeviceListModel` 订阅并切线程 |
| 契约 DLL 进 `plugins/` | `AssemblyLoadContext` 造出第二份类型，插件**静默全部注册失败** | 四个共享契约永不复制进 `plugins/` |
| 同一单例注册两次（不用工厂转发） | 页面看到的和后台跑的不是同一个对象，状态永远对不上 | `AddSingleton<T>()` + `AddSingleton<I>(sp => sp.GetRequiredService<T>())` |

### 7.4 语义与配置

| 事故 | 症状 | 正确做法 |
|---|---|---|
| 协议名子串选色 | 新增协议时颜色乱跳 | ProtocolId 稳定哈希分槽 |
| 地址里写站号前缀 | `1:40001` 被当成地址 | 设备配置站号；前缀报错 |
| **`TransportUnavailable` 被判为配置错** | 宿主 `plugins/` 少拷一个 DLL → 添设备失败 → **本地配置被删**；装好插件重启后设备已不见 | 归入 `Unreachable`。引擎三处（`ShouldAttemptReconnect` / `ProtocolProbe` / `RouteAssembler`）都当它是暂态，SDK 不能单独反着来。由 `Tests/RegisterFailureClassifyTests` 锁定 |
| **事件驱动改造后首次刷新丢失** | `Load()` 即发即忘、不缓存；订阅者尚未实例化，事件发完就没了。界面只剩本地配置、全部离线，要手动点刷新 | 组合根里**先解析订阅者再触发**。这类 bug 编译与单元测试都发现不了 |

上一代 `old/` 已不在版本库：`git checkout archive/legacy-solution -- old/`。

---

## 八、容易写错的语义

### `MinIoIntervalMs`：0 是有含义的取值，不是"未设置"

`RouteAssembler` 的判定是：

```csharp
int minIo = cmd.MinIoIntervalMs > 0
    ? cmd.MinIoIntervalMs                                  // 调用方明确要求
    : (isSerial ? defaultSerialMinIoIntervalMs : 0);       // 交由引擎按介质决定
```

所以：

| 传入 | 结果 |
|---|---|
| `0` | 串口取 `appsettings` 的 `EngineRuntime:DefaultSerialMinIoIntervalMs`（出厂 15ms）；**TCP 不节流** |
| `100` | **串口和 TCP 都被塞进 100ms 帧间静默** = 10 次 I/O／秒 |

`IHostingClient.RegisterRouteAsync` 的签名默认值是 `minIoIntervalMs = 100`。**省略这个参数不等于"用默认"，等于"明确要求 100ms"。** 调用方必须显式传 `0` 或设备实际配置值。

### `length`：单位是字节

插件自行换算到本协议的计数单位（Modbus 寄存器、MEWTOCOL 字）。奇数长度不得静默向下取整——那会让读 3 字节变成读 2 字节且不报错。

### `FrameReader` 的残留缓冲：作用域是"一次帧读取之内"

| 阶段 | 对残留的正确处理 |
|---|---|
| `ReadFrameAsync` 内部 | **消费**——那是上次多读到的、属于本帧的开头字节，丢了本帧会被截断 |
| 发送新请求之前 | **丢弃**——那时缓冲里只可能是重复或迟到的响应，留着会让请求/响应永久错位 |

同一个字段，两种截然相反的处理，取决于处在哪个阶段。而 `DiscardResidual()` 只能清 `FrameReader` 自己的缓冲，**够不着 socket 的内核缓冲**——那是 `_desynced` 与 `DrainSocketBuffer()` 存在的理由。

### `IHostedService.StartAsync` 是串行 `await` 的

在里面 `await` 长循环，排在后面的 HostedService 与 `ApplicationStarted` 回调都不会执行。一律 `Task.Run` 起循环后立即返回 `Task.CompletedTask`，令牌用 `CreateLinkedTokenSource` 串联宿主停机令牌。

---

## 九、仓库级约定

### 行尾

由 `.gitattributes` 强制（git 层，不依赖编辑器），与 `.editorconfig` 的 `end_of_line = crlf` 配套。默认 CRLF，三条例外：

| 例外 | 原因 |
|---|---|
| `*.sh` / `*.bash` / `Dockerfile` → LF | **功能要求**。CRLF 会让 shebang 变成 `#!/usr/bin/env bash\r`，Linux 报 `bad interpreter` |
| `*.yml` / `*.yaml` → LF | CI 跑在 Linux runner |
| `Tests/ApiBaselines/*.txt` → LF | 机器生成，`string.Join("\n", …)` 固定 LF |

二进制类型显式声明 `binary`；`*.cs *.razor *.xaml *.md` 显式声明 `text`——这些文件含大量中文，被启发式误判为二进制会**直接损坏内容且不报错**。

**改 `.gitattributes` 必须同步改 `.editorconfig`**，只改一边会出现"编辑器写 LF、git 检出 CRLF"的拉锯。

### API 基线

`Hosting.Sdk` 与 `Core.EngineRuntime` 会作为包发给外部消费者，公共成员即承诺。改动后：

```bash
UPDATE_API_BASELINE=1 dotnet test CommunicationKernel.Tests -c Release --filter PublicApiSurface
```

把基线 diff 一并提交并在评审里确认。**先确认它确实失败再重生成**——不失败说明基线没起作用。

### 测试

```bash
dotnet build CommunicationKernel.slnx -c Release      # TreatWarningsAsErrors=true
dotnet test CommunicationKernel.Tests -c Release
```

**测试项目是 `net8.0`，`UI.Wpf` 是 `net8.0-windows`，框架不兼容因而无法引用。WPF 层零自动化覆盖**，改动只能靠编译 + 手动冒烟验证。要补覆盖须给测试项目加 `net8.0-windows` 目标或单开一个 `Tests.Wpf` 项目。
