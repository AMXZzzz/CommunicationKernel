# Build outputs
bin/
obj/

# Visual Studio
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates

# Rider / VS Code
.idea/
.vscode/

# NuGet
*.nupkg
*.snupkg

# Logs
*.log

# Temp
.tmp/

# OS
.DS_Store
Thumbs.db

# Test output
TestResults/

# Local-only leftover folders (removed from git but may exist on disk)
CommunicationKernel.Plugin.Modbus.Tcp/
CommunicationKernel.Plugins.Siemens.S7_1200/
CommunicationKernel.Plugins.Siemens.S7_200Smart/

# ============================================================================
# 遗留解决方案
# old/ 是被当前实现取代的旧版本（13 个项目 + 两个模拟器）。
# 保留在磁盘上供查阅，但不纳入版本控制——它会污染全局搜索、
# 代码行统计与 IDE 的符号索引，并诱使新代码向已废弃的写法看齐。
# 需要归档请打 tag，而不是长期留在工作树里。
# ============================================================================
old/

# 发布产物（dotnet publish -o ./publish/...）
publish/

# 安装包产物（每次 55 MB，不入库）
installer/output/
