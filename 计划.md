# CommunicationKernel — 项目计划与进度存档

> ⚠️ 本文件用于会话切换时快速恢复上下文，新会话必须先阅读此文件再动手。

---

## 一、架构总览

### 分层链路（严格约束，不可打破）
```
UI层 -> 唯一入口Host -> 通讯业务层 -> 路由分发 -> 通讯层 -> 协议层(DLL) -> 通讯介质层
```

### 各层职责边界
| 层级 | 项目 | 允许知道 | 严禁知道 |
|------|------|----------|----------|
| UI层 | WPF/Web/Mac（待建） | route_id、业务视图模型、gRPC DTO | 协议字段、帧格式 |
| Host入口层 | `CommunicationKernel.EngineHost` | route_id、运行策略（节流/重连/状态） | 协议语义 |
| 装配服务层 | `EngineHost/Host/PluginRouteAssemblyService` | 插件发现、工厂匹配规则 | 协议帧格式 |
| 路由层 | `CommunicationKernel.Engine.Router` | RouteKey、并发调度、读写合并 | 协议语义 |
| 传输层 | `CommunicationKernel.Communication.Transport` | 字节流收发、连接生命周期 | 协议编解码 |
| 协议层 | **插件DLL（唯一允许）** | 协议全部语义、帧编解码、地址解析 | — |
| 介质层 | Transport插件（待建） | 串口/TCP/WiFi/蓝牙物理收发 | — |

### 核心约束（代码审查红线）
- **协议解析只能在插件DLL内部**，其他任何层禁止出现任何协议解析代码。
- Host层（HostRuntime）不持有任何具体协议知识，只透传 `ProtocolId` 作为插件选择键。
- 通讯业务层统一维护所有PLC状态与任务；通讯层只负责字节收发。
- 插件更新策略：重启生效（无热重载要求）。
- 代码标准：企业级，需要详细文件头注释和分支级注释，`TreatWarningsAsErrors=true`。

---

## 二、解决方案结构（当前状态）

```
CommunicationKernel.slnx
├── CommunicationKernel.Core.Abstractions          ✅ 完成
│   ├── Results/OperationResult.cs
│   ├── Results/OperationResultOfT.cs
│   ├── Errors/KernelErrorCode.cs
│   └── Versioning/KernelVersions.cs               (PluginApiVersion = 1)
├── CommunicationKernel.Core.Runtime               ✅ 完成
├── CommunicationKernel.Communication.Protocol     ✅ 完成（抽象层）
│   └── Abstractions/
│       ├── IProtocolDriver.cs
│       ├── IProtocolDriverFactory.cs
│       └── ProtocolMetadata.cs
├── CommunicationKernel.Communication.Transport    ✅ 完成（抽象层）
│   └── Abstractions/
│       ├── ITransportClient.cs
│       ├── ITransportFactory.cs
│       ├── TransportEndpoint.cs
│       └── TransportKind.cs
├── CommunicationKernel.Plugin.Runtime             ✅ 完成
│   ├── Abstractions/ (IPluginManifest/PluginDescriptor/PluginKind)
│   └── Loader/ (PluginCatalog/PluginLoadContext/PluginLoadResult/PluginValidationResult)
├── CommunicationKernel.Engine.Router              ✅ 完成
│   ├── Abstractions/ (RouteEntry/IConnectionRouter/IRouterOrchestrator...)
│   ├── Models/ (RouteKey/ReadRequestKey...)
│   └── Runtime/ (ConnectionRouter/RouterOrchestrator/WriteScheduler/ReadCoordinator/SubscriptionHub)
├── CommunicationKernel.Contracts                  ✅ 完成
│   └── Models/ (Common/ReadWrite/Routes/Subscription/Diagnostics/UI)
├── CommunicationKernel.EngineHost                 ✅ 完成
│   ├── Host/
│   │   ├── HostRuntime.cs                         (路由生命周期+节流+重连+状态发布，不含协议)
│   │   ├── EngineHostFacade.cs
│   │   ├── IRouteAssemblyService.cs               (装配抽象接口)
│   │   └── PluginRouteAssemblyService.cs          (插件装配实现)
│   ├── Services/Grpc/EngineHostGrpcService.cs     (route_id驱动，协议无关)
│   ├── Protos/V1/engine_host.proto
│   ├── Program.cs                                 (组合根DI)
│   └── appsettings.json
├── CommunicationKernel.Plugins.Modbus.Tcp         ⚠️ 仅骨架（skeleton only）
├── CommunicationKernel.Plugins.Modbus.Rtu         ⚠️ 仅骨架
├── CommunicationKernel.Plugins.Modbus.Ascii       ⚠️ 仅骨架
├── CommunicationKernel.Plugins.Panasonic.MewtocolTcp ⚠️ 仅骨架
├── CommunicationKernel.Plugins.Siemens.S7_1200    ⚠️ 骨架，★待与S7_200Smart合并★
├── CommunicationKernel.Plugins.Siemens.S7_200Smart ⚠️ 骨架，★合并后删除此项目★
└── CommunicationKernel.Tests                      ✅ 32个测试全通过
```

### 已知待清理
- `CommunicationKernel.Plugin.Modbus.Tcp`（无s）是早期误创建的孤立文件夹，需删除。

---

## 三、关键接口契约（实现插件时必须遵守）

### IProtocolDriver
```csharp
// CommunicationKernel.Communication.Protocol/Abstractions/IProtocolDriver.cs
Task<OperationResult<byte[]>> BuildReadFrameAsync(string address, int length, CancellationToken ct);
Task<OperationResult<byte[]>> BuildWriteFrameAsync(string address, byte[] payload, CancellationToken ct);
Task<OperationResult<byte[]>> ReadAsync(ITransportClient client, string address, int length, CancellationToken ct);
Task<OperationResult<byte[]>> WriteAsync(ITransportClient client, string address, byte[] payload, CancellationToken ct);
```

### ITransportClient（插件收发字节的唯一途径）
```csharp
Task<OperationResult<byte[]>> SendAndReceiveAsync(byte[] request, CancellationToken ct);
```

### IPluginManifest（每个插件DLL必须有一个实现类）
```csharp
PluginDescriptor Descriptor { get; }
// Descriptor.ApiVersion 必须 == 1 (KernelVersions.PluginApiVersion)
```

---

## 四、待完成工作清单

### Step-A（★下一步立即执行★）：合并S7插件
将 `S7_1200` 和 `S7_200Smart` 合并为 **`CommunicationKernel.Plugins.Siemens.S7`**：
- 内部两个工厂：`SiemensS7_1200ProtocolDriverFactory`（ProtocolId: `siemens-s7-1200`）
  和 `SiemensS7_200SmartProtocolDriverFactory`（ProtocolId: `siemens-s7-200smart`）
- 共享TPKT/COTP基础帧工具类，各自实现机型差异
- 更新 slnx：移除旧两项目，添加合并项目
- 更新 Tests 引用

### Step-B：实现所有协议插件（企业可用级：含诊断与状态）

#### Modbus TCP（`modbus-tcp`，端口502，MBAP Header）
- MBAP头：TransactionId(2B) + ProtocolId(2B,固定0x00) + Length(2B) + UnitId(1B)
- FC03读保持寄存器 / FC01读线圈 / FC06写单寄存器 / FC10写多寄存器
- 响应解析 + 异常码映射（01:非法功能码, 02:非法地址, 03:非法数据, 04:设备故障）
- 地址解析：支持 `40001`/`4x` 前缀 / 纯偏移数字

#### Modbus RTU（`modbus-rtu`，串口）
- CRC16（多项式0xA001）追加到帧尾
- 同FC03/FC01/FC06/FC10
- 站号（SlaveId）从address字段解析或从EndpointProperties取

#### Modbus ASCII（`modbus-ascii`，串口）
- LRC校验
- ASCII编码帧：`:StationFunctionDataLRCCRLF`

#### Panasonic Mewtocol TCP（`panasonic-mewtocol-tcp`）
- 帧格式：`%` + 站号(2) + 命令 + 数据区地址 + 数据 + `**CR`
- 数据区：DT（数据寄存器）/ WX/WY/WR（IO/辅助继电器）/ LD（链接继电器）
- 响应：`%` + 站号 + `$` + 命令 + 数据 + `**CR`
- 错误码：`!` 开头响应映射

#### Siemens S7（`siemens-s7-1200` / `siemens-s7-200smart`，TCP端口102）
- TPKT（4B）+ COTP（ISO 8073）+ S7Comm
- Connect: COTP CR/CC PDU，连接时发送ISO Connect + S7 Setup Communication
- Read Var：Item selector（DB区/I/Q/M/T/C），支持Byte/Word/DWord/Bit
- Write Var：同上
- S7-1200 vs 200Smart差异：COTP连接参数（TSAP）+ PLC Type字节

### Step-C：传输介质插件
- `CommunicationKernel.Plugins.Transport.Tcp`：封装 `System.Net.Sockets.TcpClient`
- `CommunicationKernel.Plugins.Transport.SerialPort`：封装 `System.IO.Ports.SerialPort`

### Step-D：测试补充
- 各协议帧构建单元测试
- 地址解析边界测试
- 异常码分支测试

---

## 五、Git 信息
- 仓库：`https://github.com/AMXZzzz/CommunicationKernel`
- 分支：`master`
- 根目录：`C:\Users\E.T\Desktop\通讯调试工具\`

---

## 六、新会话接手时执行顺序
1. 阅读本文件，确认架构约束。
2. 执行 Step-A：合并S7插件。
3. 执行 Step-B：依次实现各协议插件（Modbus TCP → RTU → ASCII → Mewtocol → S7）。
4. 执行 Step-C：传输插件。
5. 执行 Step-D：测试。
6. 全量构建验证（`TreatWarningsAsErrors=true` 必须通过）。
7. 推送到 GitHub。

企业级上位机通信内核（.NET 8），面向多 UI 并发访问同一批 PLC 的场景。

## 架构目标

- 多端 UI（WPF/WebUI/Mac/Android/鸿蒙/Linux）并行接入
- Host 作为统一入口（高性能 gRPC）
- Router 统一并发调度（同路由串行写、同键读合并、跨路由并行）
- Protocol / Transport 抽象解耦
- 插件 DLL 扩展协议与介质能力（重启生效）
- 全局严格质量门禁：`TreatWarningsAsErrors=true`

## 解决方案项目

- `CommunicationKernel.Core.Abstractions`：错误码、结果模型、版本契约
- `CommunicationKernel.Core.Runtime`：运行时承载（持续完善）
- `CommunicationKernel.Communication.Transport`：传输层抽象
- `CommunicationKernel.Communication.Protocol`：协议层抽象
- `CommunicationKernel.Plugin.Runtime`：插件发现/校验/隔离加载
- `CommunicationKernel.Engine.Router`：路由与并发调度核心
- `CommunicationKernel.EngineHost`：Host 门面与 gRPC 服务
- `CommunicationKernel.Contracts`：跨层 DTO 契约
- `CommunicationKernel.Tests`：核心行为测试

## 当前 gRPC 能力（EngineHost）

- `Health`
- `GetDiagnostics`
- `QueryRoutes`
- `Read`
- `Write`
- `RegisterRoute`（当前阶段未启用真实组装，严格返回阶段性失败）

## 构建

```bash
dotnet build CommunicationKernel.slnx
```

## 备注

- 本仓库已初始化独立 Git 仓库，并关联远程：
  - `origin = https://github.com/AMXZzzz/CommunicationKernel.git`
