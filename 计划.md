# CommunicationKernel — 项目计划与进度存档

> ⚠️ 本文件用于会话切换时快速恢复上下文，新会话必须先阅读此文件再动手。

---

## 一、架构总览

### 分层链路（严格约束，不可打破）

```
┌─────────────────────────────────────────────────────────────┐
│  UI 层（WPF / WebUI / Mac / Android / 鸿蒙）                 │
│  ─ 只持有 route_id 字符串，通过 gRPC channel 调用            │
└────────────────────┬────────────────────────────────────────┘
                     │  gRPC (HTTP/2, Protobuf)
┌────────────────────▼────────────────────────────────────────┐
│  EngineHostGrpcService                                        │
│  ─ 将 gRPC 请求翻译为 HostRuntime 方法调用，不做业务逻辑     │
└────────────────────┬────────────────────────────────────────┘
                     │  方法调用（RegisterRoute / Read / Write）
┌────────────────────▼────────────────────────────────────────┐
│  HostRuntime（Host 入口层）                                   │
│  ─ 路由生命周期管理（注册/注销/状态发布）                    │
│  ─ 企业级运行策略：串口节流、单次重连、状态流推送             │
│  ─ 严禁知道协议细节；只知道 route_id → RouteKey 的映射       │
└────────────────────┬────────────────────────────────────────┘
                     │  ExecuteReadAsync / ExecuteWriteAsync
┌────────────────────▼────────────────────────────────────────┐
│  RouterOrchestrator（路由层门面）                             │
│  ─ 聚合下方四个子组件，提供单一入口供 HostRuntime 调用       │
│  ┌──────────────┬──────────────┬───────────────────────┐    │
│  │ConnectionRouter│WriteScheduler│ ReadCoordinator       │    │
│  │（路由注册表） │（写串行化）  │ （读请求合并）        │    │
│  └──────────────┴──────────────┴───────────────────────┘    │
│  ┌───────────────────────────────────────────────────────┐  │
│  │SubscriptionHub（状态订阅广播，Task.WhenAll 并行扇出）  │  │
│  └───────────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────────┘
                     │  ReadAsync / WriteAsync（传入 ITransportClient）
┌────────────────────▼────────────────────────────────────────┐
│  IProtocolDriver（协议层 / 插件 DLL 内部）                   │
│  ─ BuildReadFrame / BuildWriteFrame（同步，纯 CPU）          │
│  ─ ReadAsync / WriteAsync（异步，调用 Transport 收发字节）   │
│  ─ 协议帧格式、地址解析、校验、握手全部在此层，外层禁知      │
└────────────────────┬────────────────────────────────────────┘
                     │  SendAndReceiveAsync(byte[]) → byte[]
┌────────────────────▼────────────────────────────────────────┐
│  ITransportClient（传输层 / Transport 插件）                  │
│  ─ 负责字节级收发，不解释内容                                │
│  ─ TCP：TcpClient + NetworkStream；Serial：SerialPort        │
└─────────────────────────────────────────────────────────────┘
```

### 各层职责边界（红线，审查必查）

| 层级 | 项目 | 允许知道 | 严禁知道 |
|------|------|----------|----------|
| UI层 | WPF/Web/Mac（待建） | route_id、视图模型、gRPC DTO | 协议字段、帧格式、地址格式 |
| gRPC入口 | `EngineHostGrpcService` | gRPC 请求/响应 DTO、HostRuntime 公开方法 | HostRuntime 内部字段、Facade |
| Host入口层 | `HostRuntime` | route_id、RouteKey、运行策略（节流/重连/状态） | 协议帧内容、协议地址解析规则 |
| 装配服务层 | `PluginRouteAssemblyService` | 插件工厂接口、TransportKind、ProtocolId | 协议帧格式 |
| 路由层 | `Engine.Router` | RouteKey、RouteEntry（不透明）、并发调度 | 协议语义、地址含义 |
| 传输层 | `Communication.Transport` | 字节流、连接生命周期 | 协议编解码 |
| 协议层 | **插件DLL（唯一允许）** | 协议全部语义、帧编解码、地址解析、校验 | 业务逻辑 |

---

## 二、核心数据流

### 2.1 路由注册流程

```
UI/gRPC → RegisterRouteAsync(RegisterRouteCommand)
  │
  ├─ [校验] RouteId 重复 → OperationResult.Fail(RouteBusy)
  │
  ├─ PluginRouteAssemblyService.AssembleAsync(command)
  │    ├─ 查找 IProtocolDriverFactory（按 ProtocolId 匹配）
  │    ├─ 查找 ITransportFactory（按 TransportKind / TransportId 匹配）
  │    ├─ 构建 RouteKey(ProtocolId, TransportKind, Address, Port, Station)
  │    ├─ transportFactory.CreateClient()
  │    ├─ transportClient.ConnectAsync(endpoint)   ← 真实 TCP/串口连接
  │    ├─ protocolFactory.CreateDriver()           ← 创建无状态驱动实例
  │    └─ 返回 RouteAssemblyResult{ RouteEntry, RollbackAsync }
  │
  ├─ [校验] RouteKey 重复 → RollbackAsync() → Fail(RouteBusy)
  │
  ├─ Facade.TryRegister(routeEntry)
  │    └─ ConnectionRouter.TryRegister(entry)  ← ConcurrentDictionary
  │
  ├─ 记录三张字典：
  │    _registrationsByRouteId[routeId] = RouteRuntimeRegistration
  │    _routeIdByKey[routeKey]          = routeId
  │    _routeStatuses[routeId]          = RouteStatusSnapshot(Online=true)
  │
  └─ PublishStatus(routeId, online=true) → RouteStatusChanged?.Invoke(snapshot)
```

### 2.2 读取流程（ReadByRouteIdAsync）

```
UI/gRPC → ReadByRouteIdAsync(routeId, address, length)
  │
  ├─ 查 _registrationsByRouteId → RouteRuntimeRegistration
  │
  ├─ Facade.ExecuteReadAsync(ReadRequestKey, readAction)
  │    └─ ReadCoordinator.ExecuteAsync(requestKey, readAction)
  │         ├─ ReadRequestKey = (RouteKey, address, length)
  │         ├─ 相同 Key 的并发请求合并为一个 Lazy<Task>（避免重复 IO）
  │         └─ 返回时所有等待者得到相同结果
  │
  ├─ readAction = ExecuteWithRoutePolicyAsync(registration, ioAction)
  │    ├─ [串口节流] IsSerialRoute → WaitSerialWindowAsync
  │    │    ├─ SerialIoGate.WaitAsync()（同路由串口访问串行化）
  │    │    └─ Task.Delay(MinIoIntervalMs - elapsed)（帧间静默）
  │    │
  │    ├─ ioAction: ProtocolDriver.ReadAsync(transportClient, address, length, ct)
  │    │    ├─ BuildReadFrame(address, length)  ← 同步，构建请求字节帧
  │    │    ├─ transportClient.SendAndReceiveAsync(requestBytes)  ← TCP/串口收发
  │    │    └─ ParseXxxResponse(responseBytes)  ← 协议内部解析，外层不可见
  │    │
  │    ├─ [成功] PublishStatus(online=true)
  │    ├─ [IO 错误，需重连] TryReconnectAsync → 重试一次 ioAction
  │    └─ [失败] PublishStatus(online=false, errorCode, errorMessage)
  │
  └─ 返回 OperationResult<byte[]>
```

### 2.3 写入流程（WriteByRouteIdAsync）

```
UI/gRPC → WriteByRouteIdAsync(routeId, address, payload)
  │
  ├─ 查 _registrationsByRouteId → RouteRuntimeRegistration
  │
  ├─ Facade.ExecuteWriteAsync(routeKey, writeAction)
  │    └─ WriteScheduler.ScheduleAsync(routeKey, writeAction)
  │         ├─ 同一 RouteKey 的写操作串行化（SemaphoreSlim(1,1)）
  │         └─ 不同 RouteKey 并行执行（每个 Key 独立信号量）
  │
  ├─ writeAction = ExecuteWithRoutePolicyAsync(registration, ioAction)
  │    ├─ [同读取：串口节流 + 重连策略]
  │    └─ ioAction: ProtocolDriver.WriteAsync(transportClient, address, payload, ct)
  │         ├─ BuildWriteFrame(address, payload)  ← 同步，构建写帧字节
  │         ├─ transportClient.SendAndReceiveAsync(requestBytes)
  │         └─ ParseWriteResponse(responseBytes)
  │
  └─ 返回 OperationResult
```

### 2.4 路由注销流程（UnregisterRouteAsync）

```
UI/gRPC → UnregisterRouteAsync(routeId)
  │
  ├─ _registrationsByRouteId.TryRemove(routeId) → RouteRuntimeRegistration
  ├─ _routeIdByKey.TryRemove(routeKey)
  ├─ _routeStatuses.TryRemove(routeId)
  │
  └─ Orchestrator.TryRemoveAndDisposeAsync(routeKey)   ← 原子清理
       ├─ ConnectionRouter.TryRemove(key)              ← 从路由表移除
       ├─ WriteScheduler.Release(key)                  ← 释放 SemaphoreSlim（防内存泄漏）
       └─ entry.DisposeAsync()                         ← 关闭 TCP socket / 串口句柄
```

### 2.5 状态订阅流程（WatchRouteStatus gRPC 流）

```
UI/gRPC 建立 Server-Streaming → WatchRouteStatus(routeId?)
  │
  ├─ 创建 BoundedChannel<RouteStatusSnapshot>(capacity=256, DropOldest)
  │   └─ 防止慢客户端导致 OOM
  │
  ├─ HostRuntime.RouteStatusChanged += 写入 Channel
  │   └─ 每次 ReadByRouteId / WriteByRouteId 完成后 → PublishStatus → 触发事件
  │
  ├─ 先发送所有当前状态快照（初始化）
  │
  └─ 循环 Channel.Reader.ReadAllAsync → gRPC stream.WriteAsync
       └─ 客户端断开 / CancellationToken 触发 → 移除事件订阅，结束流
```

---

## 三、Router 层四组件详解

### 3.1 ConnectionRouter（路由注册表）

**作用**：`RouteKey → RouteEntry` 的线程安全键值存储。

```
职责：注册 / 查询 / 移除 / 快照
实现：ConcurrentDictionary<RouteKey, RouteEntry>
注意：TryRemove 只从字典移除，不释放资源（资源释放由 TryRemoveAndDisposeAsync 统一处理）
```

| 方法 | 说明 |
|------|------|
| `TryRegister(entry)` | 同一 RouteKey 只能注册一次，重复返回 false，并记录 Warning 日志 |
| `TryGet(key, out entry)` | 查找，不存在返回 false |
| `TryRemove(key, out removed)` | 移除并输出被移除的 entry（供调用方 Dispose） |
| `Snapshot()` | 返回当前所有 entry 的只读快照（用于 gRPC QueryRoutes） |
| `Count` | 当前路由数量 |

### 3.2 WriteScheduler（写操作串行化调度器）

**作用**：同一设备（RouteKey）的并发写操作串行化，不同设备并行执行。

```
实现：ConcurrentDictionary<RouteKey, SemaphoreSlim>（每个 Key 独立信号量）
模式：每次写前 GetOrAdd 信号量，WaitAsync → 执行写 → Release
```

| 方法 | 说明 |
|------|------|
| `ScheduleAsync(routeKey, writeAction, ct)` | 对指定路由串行化，内部用 SemaphoreSlim(1,1) |
| `Release(routeKey)` | 路由注销时调用，移除并 Dispose 对应信号量（防内存泄漏） |

**为什么需要串行化**：同一 PLC 设备不能同时接收多帧请求，否则响应帧错位。

### 3.3 ReadCoordinator（读请求合并）

**作用**：完全相同的读请求（同 RouteKey + 同地址 + 同长度）并发时只发出一次 IO，结果共享。

```
Key：ReadRequestKey = (RouteKey, address, length)
实现：ConcurrentDictionary<ReadRequestKey, Lazy<Task<OperationResult<byte[]>>>>
设计决策：不同 length 的同地址请求视为独立请求，不做跨长度合并
```

| 方法 | 说明 |
|------|------|
| `ExecuteAsync(requestKey, readAction, ct)` | GetOrAdd Lazy<Task>，第一个调用者真正执行，后续等待同一 Task |

### 3.4 SubscriptionHub（状态订阅广播）

**作用**：路由状态变更时通知所有已订阅的 UI 客户端。

```
实现：ConcurrentDictionary<Guid, (SubscriptionTopic, Handler)>
扇出：Task.WhenAll（并行通知所有订阅者，消除头部阻塞）
异常隔离：SafeInvokeAsync 捕获业务异常并记录 LogError，不影响其他订阅者
取消传播：OperationCanceledException 不被吞没，直接 rethrow
```

| 方法 | 说明 |
|------|------|
| `Subscribe(topic, handler)` | 注册处理器，返回 Guid 作为注销凭证 |
| `Unsubscribe(subscriptionId)` | 按 Guid 移除 |
| `PublishAsync(topic, payload, ct)` | 并行调用所有匹配 topic 的处理器 |
| `Count` | 当前有效订阅数 |

---

## 四、关键模型定义

### RouteKey（路由唯一标识）

```csharp
readonly record struct RouteKey(
    string ProtocolId,      // 例："modbus-tcp"
    TransportKind Kind,     // Tcp / Serial
    string Address,         // IP 或串口名，例："192.168.1.10" / "COM3"
    int Port,               // TCP 端口，Serial 时为 0
    string? Station);       // 站号/从站ID，例："1"；可空

// 字符串表示：{ProtocolId}|{Kind}|{Address}:{Port}|{Station}
// 例："modbus-tcp|Tcp|192.168.1.10:502|1"
```

**注意**：RouteKey 中 `|` 是分隔符，Address / ProtocolId / Station 中不能包含 `|`。

### RouteEntry（路由运行时实体）

```csharp
sealed class RouteEntry : IAsyncDisposable {
    RouteKey         Key;              // 不可变标识
    ITransportClient TransportClient;  // TCP socket / SerialPort 包装
    IProtocolDriver  ProtocolDriver;   // 协议实例（内含握手状态，如 S7 handshakeDone）
}
// DisposeAsync() → TransportClient.DisposeAsync()（关闭底层句柄）
```

### RouteRuntimeRegistration（HostRuntime 内部记录）

```csharp
// 仅存在于 HostRuntime，Router 层不感知
sealed class RouteRuntimeRegistration {
    string           RouteId;          // 用户指定的人类可读名称
    RouteKey         RouteKey;         // 路由器内部键
    TransportEndpoint Endpoint;        // 重连时需要的端点信息
    string           TransportId;      // 传输插件标识（查询用）
    RouteEntry       RouteEntry;       // 路由实体引用
    bool             IsSerialRoute;    // 是否串口（决定是否需要节流）
    int              MinIoIntervalMs;  // 串口最小帧间隔（默认 15ms）
    SemaphoreSlim    SerialIoGate;     // 串口访问串行化门
    // 时间追踪：Interlocked long ticks 记录最后一次串口 IO 完成时间
}
```

---

## 五、接口契约（实现插件时必须遵守）

### IProtocolDriver

```csharp
public interface IProtocolDriver {
    ProtocolMetadata Metadata { get; }

    // 同步（帧构建是纯 CPU 工作，无 I/O，不需要 Task 分配）
    OperationResult<byte[]> BuildReadFrame(string address, int length);
    OperationResult<byte[]> BuildWriteFrame(string address, byte[] payload);

    // 异步（需通过 TransportClient 发送/接收字节）
    Task<OperationResult<byte[]>> ReadAsync(
        ITransportClient client, string address, int length, CancellationToken ct);
    Task<OperationResult> WriteAsync(
        ITransportClient client, string address, byte[] payload, CancellationToken ct);
}
```

**ReadAsync 标准实现模式**：
```csharp
public async Task<OperationResult<byte[]>> ReadAsync(...) {
    // 1. 构建请求帧（同步）
    OperationResult<byte[]> frame = BuildReadFrame(address, length);
    if (!frame.Success) return frame;

    // 2. 字节收发（委托 Transport 层）
    OperationResult<byte[]> response =
        await client.SendAndReceiveAsync(frame.Value, ct).ConfigureAwait(false);
    if (!response.Success) return response;

    // 3. 解析响应（协议内部，不暴露给外层）
    return ParseXxxResponse(response.Value, length);
}
```

### ITransportClient

```csharp
public interface ITransportClient : IAsyncDisposable {
    Task<OperationResult> ConnectAsync(TransportEndpoint endpoint, CancellationToken ct);
    Task DisconnectAsync(CancellationToken ct);
    Task<OperationResult<byte[]>> SendAndReceiveAsync(byte[] request, CancellationToken ct);
}
```

**调用链**：`ProtocolDriver.ReadAsync → TransportClient.SendAndReceiveAsync → TCP NetworkStream / SerialPort`

### IProtocolDriverFactory

```csharp
public interface IProtocolDriverFactory {
    ProtocolMetadata Metadata { get; }  // 含 ProtocolId（插件注册键）
    IProtocolDriver CreateDriver();     // 每路由创建独立实例（含独立握手状态）
}
```

### ITransportFactory

```csharp
public interface ITransportFactory {
    string TransportId { get; }        // 例："tcp-default"
    TransportKind Kind { get; }        // Tcp / Serial
    ITransportClient CreateClient();   // 每路由创建独立实例
}
```

### IPluginManifest（每个插件 DLL 必须实现）

```csharp
public interface IPluginManifest {
    PluginDescriptor Descriptor { get; }
    // Descriptor.ApiVersion 必须 == 1 (KernelVersions.PluginApiVersion)
}
```

### OperationResult<T> 使用约定

```csharp
// [MemberNotNullWhen(true, nameof(Value))] 已标注
// ✅ 正确写法：Success 检查后直接访问 Value，无需 !
if (!result.Success) return result;
byte[] data = result.Value;  // 编译器静态知道非 null

// ❌ 错误写法：不检查 Success 直接访问
byte[] data = result.Value!;  // 可能运行时 null

// 错误码约定
OperationResult.Fail(message, KernelErrorCode.RouteNotFound)
OperationResult.Fail(message, KernelErrorCode.RouteBusy)
OperationResult.Fail(message, KernelErrorCode.ProtocolError)
OperationResult.Fail(message, KernelErrorCode.TransportIoError)
OperationResult.InvalidArgument(message)
```

---

## 六、日志系统设计

### 日志注入链

```
AppMain.cs
  └─ ILoggerFactory（ASP.NET Core 内置 DI）
       ├─ HostRuntime(ILogger<HostRuntime>?, ILoggerFactory?)
       │    └─ RouterOrchestrator(ILoggerFactory?)
       │         ├─ ConnectionRouter(ILogger<ConnectionRouter>?)
       │         ├─ WriteScheduler(ILogger<WriteScheduler>?)
       │         └─ SubscriptionHub(ILogger<SubscriptionHub>?)
       └─ PluginRouteAssemblyService(ILoggerFactory?)
            └─ PluginCatalog(ILogger<PluginCatalog>?)
```

### NullLogger 默认模式（测试友好）

```csharp
// 所有组件构造函数均可不传 logger，测试用例无需 Mock
public WriteScheduler(ILogger<WriteScheduler>? logger = null) {
    _logger = logger ?? NullLogger<WriteScheduler>.Instance;
}

// 生产代码通过 DI / ILoggerFactory 注入真实 logger
// 测试代码：new WriteScheduler() 即可，不需要传 null logger
```

### 日志级别约定

| 级别 | 场景 |
|------|------|
| `LogInformation` | 路由注册成功、重连成功、初始化完成 |
| `LogWarning` | 路由重复注册被拒绝、IO 失败触发重连、状态变为离线 |
| `LogError` | 插件加载失败、重连失败、ProtocolDriver 未处理异常 |
| `LogDebug` | WriteScheduler 信号量释放、SubscriptionHub 细节扇出 |

---

## 七、协议插件地址格式速查

### Modbus TCP（`modbus-tcp`，端口 502）

| 地址格式 | 含义 | FC |
|----------|------|----|
| `40001` | 保持寄存器 0，UnitId=1 | FC03 |
| `1:40001` | 保持寄存器 0，UnitId=1（显式） | FC03 |
| `coil:5` | 线圈 5，UnitId=1 | FC01 |
| `00001` | 线圈 0（0xxxx 格式） | FC01 |
| `2:coil:5` | 线圈 5，UnitId=2 | FC01 |

- `length=1` → FC01（线圈）；`length>1` → FC03（保持寄存器）
- 写：1字节线圈 → FC05；2字节 → FC06；>2字节 → FC10

### Modbus RTU（`modbus-rtu`，串口）

地址格式同 Modbus TCP，帧尾附 CRC16（多项式 0xA001）。

### Modbus ASCII（`modbus-ascii`，串口）

地址格式同 Modbus TCP，帧格式：`: StationID FunctionCode Data LRC CR LF`，每字节编码为 2 个十六进制 ASCII 字符。

### Panasonic MEWTOCOL TCP（`panasonic-mewtocol-tcp`，端口 9094）

| 地址格式 | 含义 |
|----------|------|
| `DT100` | 数据寄存器 100，站号=1 |
| `01:DT100` | 数据寄存器 100，站号=1（显式） |
| `R10A` | R 区 word10 的第 A（10）位 |
| `R100` | R 区触点 100 |
| `X5` / `Y3` | 外部输入/输出触点 |
| `WR50` / `W50` | 链接继电器 50 |

- `length=1` 或 `IsBit` → RCS（读触点）；否则 → RD（字数=ceil(length/2)）
- 字节序：MEWTOCOL 低字节先出，读写均需 SwapBytes

### Siemens S7（`siemens-s7-1200` / `siemens-s7-200smart`，端口 102）

| 地址格式 | 含义 |
|----------|------|
| `DB10.DBW0` | DB10 字偏移 0（Word = 2字节） |
| `DB10.DBB4` | DB10 字节偏移 4 |
| `DB10.DBD8` | DB10 双字偏移 8（DWord = 4字节） |
| `DB10.0` | DB10 字节偏移 0（无前缀） |
| `MB0` / `MW2` / `MD4` | 位内存字节/字/双字 |
| `IB0` / `QB0` | 输入/输出字节 |
| `VB0` / `VW2` / `VD4` | 200Smart V区（映射为 DB1） |
| `T0` / `C0` | 定时器/计数器 |

- 握手（每个 Driver 实例首次 IO 时自动执行，Interlocked 保证只握手一次）：
  1. COTP CR 报文 → 等待 CC（`response[5] == 0xD0`，至少 6 字节）
  2. S7 Setup Communication → 验证 `[8]=0x03`、`[17]=0x00`、`[18]=0x00`、`[19]=0xF0`
- S7-1200 TSAP = 0x0300；S7-200Smart TSAP = 0x0200

---

## 八、解决方案结构（当前完成状态）

```
CommunicationKernel.slnx
├── CommunicationKernel.Core.Abstractions          ✅
│   ├── Results/OperationResult.cs                 (Ok / Fail / InvalidArgument / Timeout)
│   ├── Results/OperationResultOfT.cs              [MemberNotNullWhen(true,Value)]
│   ├── Errors/KernelErrorCode.cs
│   └── Versioning/KernelVersions.cs               (PluginApiVersion = 1)
│
├── CommunicationKernel.Core.Runtime               ✅
│   └── KernelRuntime.cs                           (TimeProvider/日志/诊断占位)
│
├── CommunicationKernel.Communication.Protocol     ✅
│   └── Abstractions/IProtocolDriver.cs            同步 BuildReadFrame / BuildWriteFrame
│
├── CommunicationKernel.Communication.Transport    ✅
│   └── Abstractions/ ITransportClient / ITransportFactory / TransportEndpoint / TransportKind
│
├── CommunicationKernel.Plugin.Runtime             ✅
│   └── Loader/PluginCatalog.cs                    单次加载（消除双重加载安全窗口）+ ILogger
│
├── CommunicationKernel.Engine.Router              ✅
│   ├── Abstractions/
│   │   ├── RouteEntry.cs                          IAsyncDisposable（释放 TransportClient）
│   │   ├── IConnectionRouter.cs                   注册/查询/移除/快照
│   │   ├── IWriteScheduler.cs                     ScheduleAsync + Release(RouteKey)
│   │   ├── IRouterOrchestrator.cs                 TryRemoveAndDisposeAsync; RouteCount; SubscriptionCount
│   │   ├── IReadCoordinator.cs                    同键请求合并
│   │   └── ISubscriptionHub.cs                    Task.WhenAll 并行扇出
│   └── Runtime/
│       ├── RouterOrchestrator.cs                  ILoggerFactory 传播；TryRemoveAndDisposeAsync 原子清理
│       ├── ConnectionRouter.cs                    ILogger<ConnectionRouter>
│       ├── WriteScheduler.cs                      bool acquired 锁追踪；Release() 释放 Semaphore
│       ├── ReadCoordinator.cs                     Lazy<Task> 读合并
│       └── SubscriptionHub.cs                     ILogger；Task.WhenAll；OperationCanceled 上传
│
├── CommunicationKernel.Contracts                  ✅
│   └── Models/ Common / ReadWrite / Routes / Subscription / Diagnostics / UI
│
├── CommunicationKernel.EngineHost                 ✅
│   ├── Host/
│   │   ├── HostRuntime.cs                         路由注册/注销/读写/重连/状态推送；Facade=internal
│   │   ├── EngineHostFacade.cs                    internal Facade；ToRouteInfo 修复；移除防御性??
│   │   ├── IRouteAssemblyService.cs               装配抽象接口
│   │   └── PluginRouteAssemblyService.cs          插件工厂发现 + 连接装配 + RollbackAsync
│   ├── Services/Grpc/EngineHostGrpcService.cs     ILogger；BoundedChannel(256)；公开属性访问
│   └── AppMain.cs                                 ILoggerFactory 注入 PluginRouteAssemblyService
│
├── 协议插件（Plugin DLLs）                        全部 ✅
│   ├── CommunicationKernel.Plugins.Modbus.Tcp     FC01/03/05/06/10，MBAP 帧
│   ├── CommunicationKernel.Plugins.Modbus.Rtu     FC01/03/05/06/10，CRC16
│   ├── CommunicationKernel.Plugins.Modbus.Ascii   FC01/03/05/06/10，LRC，':' 帧
│   ├── CommunicationKernel.Plugins.Panasonic.MewtocolTcp  RCS/RD/WCS/WD，BCC
│   └── CommunicationKernel.Plugins.Siemens.S7    TPKT/COTP/S7Comm，自动握手
│
├── 传输插件（Transport DLLs）                     全部 ✅
│   ├── CommunicationKernel.Plugins.Transport.Tcp
│   └── CommunicationKernel.Plugins.Transport.SerialPort
│
└── CommunicationKernel.Tests                      ✅  82 / 82 通过，0 warnings，0 errors
```

---

## 九、已解决的重大 Bug

| 问题 | 根因 | 修复 |
|------|------|------|
| `Value` 访问 CS8604/CS8602 | `Success` 后编译器仍认为 `Value` 可空 | 加 `[MemberNotNullWhen(true,nameof(Value))]` |
| S7 握手必然失败 | 校验位置错误：`[17]!=0xF0` 实际 `[17]` 是错误类 | 改为 `[17-18]==0x00` + `[19]==0xF0` |
| COTP CC 响应越界 | 检查 `<5` 但访问 `[5]` | 改为 `<6` |
| S7 DB 地址无前缀 Substring 越界 | `sub.Substring(3)` 在无前缀时越界 | 条件判断，无前缀直接用 `sub` |
| WriteScheduler 竞态 | `CurrentCount==0` 判断与 Release 有竞态窗口 | 改为 `bool acquired` 明确追踪 |
| 路由注销 TransportClient 句柄泄漏 | 旧代码只从字典移除，不 Dispose | `IAsyncDisposable RouteEntry` + `TryRemoveAndDisposeAsync` |
| SemaphoreSlim 注销后不释放 | `WriteScheduler` 无注销路径 | `IWriteScheduler.Release(RouteKey)` |
| SubscriptionHub 头部阻塞 | 串行调用每个 handler | `Task.WhenAll` 并行扇出 + `SafeInvokeAsync` 隔离异常 |
| PluginCatalog 双重加载 | 校验加载一次，运行加载一次，中间有文件替换窗口 | 单次加载：校验失败 → `Unload()`；成功 → 保留上下文 |
| gRPC 穿透内部结构 | `Facade` 为 `public`，gRPC 服务直接访问 `Facade.Orchestrator.ConnectionRouter` | `Facade` 改 `internal`；`RouteCount/SubscriptionCount` 公开属性 |
| BuildReadFrame 强制 Task 分配 | 帧构建是纯 CPU 同步工作却返回 `Task<>` | 改为同步 `OperationResult<byte[]>` |
| 孤立目录残留 | `CommunicationKernel.Plugin.Modbus.Tcp/` 不属于任何项目 | `git rm -r` |
| Mewtocol `ImplicitUsings=disable` | 与其他项目不一致，编译警告 | 改为 `enable` |
| LogError 具名参数 CS1739 | `topic: payload?.GetType().Name` 不合法 | 移除具名参数前缀 |

---

## 十、待完成工作清单

### Step-D：测试补充（优先级高）

- [ ] 各协议 `BuildReadFrame` / `BuildWriteFrame` **帧字节级验证**
  - ModbusTcp：`40001` → 验证 MBAP Header(TID/Length) + FC03 字节序列
  - ModbusRtu：`1:40001` → 验证 CRC16 附加
  - ModbusAscii：`40001` → 验证 `:01 03 ...` ASCII 格式 + LRC
  - MewtocolTcp：`DT100` → 验证 `%01#RDD0100...BCC\r` 格式
  - S7-1200：`DB10.DBW0` → 验证 TPKT + COTP + S7Comm Read Var 字节序列
- [ ] **地址解析边界测试**：前缀/无前缀/越界/非法字符
- [ ] **异常码分支**：Modbus 0x81-0x8B，MEWTOCOL 20-29，S7 错误类/码
- [ ] `WriteScheduler` **并发压测**：多 Route 并行、同 Route 串行保证验证
- [ ] `SubscriptionHub.Task.WhenAll` 并行扇出验证（含单处理器异常隔离）
- [ ] `HostRuntime.UnregisterRouteAsync` 释放链端到端验证

### Step-E：RouteKey 边界校验（L-02）

- [ ] `RegisterRouteAsync` 入口校验 `Address`、`ProtocolId`、`Station` 不含 `|`
- [ ] 返回 `KernelErrorCode.InvalidArgument`

### Step-F：UI 层接入

- [ ] WPF / WebUI 客户端（gRPC channel → EngineHost）
- [ ] 多端并行接入（Host 已就绪）

---

## 十一、新会话接手时执行顺序

1. `git pull` 获取最新代码
2. 阅读本文件（重点：第一节架构约束、第五节接口契约）
3. `dotnet build -warnaserror` 确认 **0 警告 0 错误**
4. `dotnet test` 确认 **82 个测试全通过**
5. 继续 Step-D → Step-E → Step-F
6. 每次修改后运行 `dotnet build -warnaserror && dotnet test`，不允许带警告合入

---

## 十二、架构目标（不变）

- 多端 UI（WPF / WebUI / Mac / Android / 鸿蒙 / Linux）并行接入
- Host 作为统一入口（高性能 gRPC，HTTP/2）
- Router 统一并发调度：同路由串行写、同键读合并、跨路由并行
- Protocol / Transport 抽象解耦，插件 DLL 扩展（重启生效）
- 全局严格质量门禁：`TreatWarningsAsErrors=true`，零 nullable 警告，全链路 `ILogger` 覆盖
