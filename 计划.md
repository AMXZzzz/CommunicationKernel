# CommunicationKernel — 项目计划与进度存档

> ⚠️ 本文件用于会话切换时快速恢复上下文，新会话必须先阅读此文件再动手。

---

## 一、架构总览

### 分层链路（严格约束，不可打破）
```
UI层 -> 唯一入口Host -> 通讯业务层 -> 路由分发 -> 通讯层 -> 协议层(DLL) -> 通讯介质层
```

### 各层职责边界
| 层级 | 项目 | 允许知道 | 严禁知道 |
|------|------|----------|----------|
| UI层 | WPF/Web/Mac（待建） | route_id、业务视图模型、gRPC DTO | 协议字段、帧格式 |
| Host入口层 | `CommunicationKernel.EngineHost` | route_id、运行策略（节流/重连/状态） | 协议语义 |
| 装配服务层 | `EngineHost/Host/PluginRouteAssemblyService` | 插件发现、工厂匹配规则 | 协议帧格式 |
| 路由层 | `CommunicationKernel.Engine.Router` | RouteKey、并发调度、读写合并 | 协议语义 |
| 传输层 | `CommunicationKernel.Communication.Transport` | 字节流收发、连接生命周期 | 协议编解码 |
| 协议层 | **插件DLL（唯一允许）** | 协议全部语义、帧编解码、地址解析 | — |
| 介质层 | Transport插件（待建） | 串口/TCP/WiFi/蓝牙物理收发 | — |

### 核心约束（代码审查红线）
- **协议解析只能在插件DLL内部**，其他任何层禁止出现任何协议解析代码。
- Host层（HostRuntime）不持有任何具体协议知识，只透传 `ProtocolId` 作为插件选择键。
- 通讯业务层统一维护所有PLC状态与任务；通讯层只负责字节收发。
- 插件更新策略：重启生效（无热重载要求）。
- 代码标准：企业级，需要详细文件头注释和分支级注释，`TreatWarningsAsErrors=true`。

---

## 二、解决方案结构（当前状态）

```
CommunicationKernel.slnx
├── CommunicationKernel.Core.Abstractions          ✅ 完成
│   ├── Results/OperationResult.cs                 (含静态工厂方法 Ok / Fail / InvalidArgument / Timeout)
│   ├── Results/OperationResultOfT.cs              ✅ [MemberNotNullWhen(true,Value)] 已加入
│   ├── Errors/KernelErrorCode.cs
│   └── Versioning/KernelVersions.cs               (PluginApiVersion = 1)
├── CommunicationKernel.Core.Runtime               ✅ 完成
│   └── KernelRuntime.cs                           (规划用途占位：TimeProvider/日志/诊断)
├── CommunicationKernel.Communication.Protocol     ✅ 完成（抽象层）
│   └── Abstractions/
│       ├── IProtocolDriver.cs
│       ├── IProtocolDriverFactory.cs
│       └── ProtocolMetadata.cs
├── CommunicationKernel.Communication.Transport    ✅ 完成（抽象层）
│   └── Abstractions/
│       ├── ITransportClient.cs                    (IAsyncDisposable)
│       ├── ITransportFactory.cs
│       ├── TransportEndpoint.cs
│       └── TransportKind.cs
├── CommunicationKernel.Plugin.Runtime             ✅ 完成
│   ├── Abstractions/ (IPluginManifest/PluginDescriptor/PluginKind)
│   └── Loader/ (PluginCatalog/PluginLoadContext/PluginLoadResult/PluginValidationResult)
├── CommunicationKernel.Engine.Router              ✅ 完成
│   ├── Abstractions/ (RouteEntry/IConnectionRouter/IRouterOrchestrator...)
│   ├── Models/ (RouteKey/ReadRequestKey/SubscriptionTopic)
│   └── Runtime/
│       ├── ConnectionRouter.cs
│       ├── RouterOrchestrator.cs
│       ├── WriteScheduler.cs                      ✅ bool acquired 锁追踪修复
│       ├── ReadCoordinator.cs                     (Lazy<Task> 读合并)
│       └── SubscriptionHub.cs
├── CommunicationKernel.Contracts                  ✅ 完成
│   └── Models/ (Common/ReadWrite/Routes/Subscription/Diagnostics/UI)
├── CommunicationKernel.EngineHost                 ✅ 完成
│   ├── Host/
│   │   ├── HostRuntime.cs                         (路由生命周期+节流+重连+状态发布，不含协议)
│   │   ├── EngineHostFacade.cs
│   │   ├── IRouteAssemblyService.cs               (装配抽象接口)
│   │   └── PluginRouteAssemblyService.cs          (插件装配实现)
│   ├── Services/Grpc/EngineHostGrpcService.cs     (route_id驱动，协议无关)
│   └── Program.cs
│
├── 协议插件（Plugin DLLs）
│   ├── CommunicationKernel.Plugins.Modbus.Tcp    ✅ 完整实现
│   │   ├── Internal/ModbusAddress.cs              (40001/4xxxx/coil:/0xxxx/裸偏移/unitId前缀)
│   │   ├── Internal/ModbusFrame.cs                (FC01/FC03/FC05/FC06/FC10 + 异常码映射)
│   │   └── ModbusTcpPlugin.cs                     (Manifest + Factory + Driver)
│   │
│   ├── CommunicationKernel.Plugins.Modbus.Rtu    ⚠️ 骨架（待实现：CRC16 + 串口帧）
│   ├── CommunicationKernel.Plugins.Modbus.Ascii  ⚠️ 骨架（待实现：LRC + ':' 帧格式）
│   │
│   ├── CommunicationKernel.Plugins.Panasonic.MewtocolTcp  ✅ 完整实现
│   │   ├── Internal/MewtocolAddress.cs            (X/Y/R/DT/WR + 站号前缀 + 位访问R10A)
│   │   ├── Internal/MewtocolFrame.cs              (RCS/RD/WCS/WD + BCC + 错误码映射)
│   │   └── MewtocolTcpPlugin.cs                   (Manifest + Factory + Driver)
│   │
│   └── CommunicationKernel.Plugins.Siemens.S7   ✅ 完整实现（S7-1200 + S7-200Smart 合并）
│       ├── Internal/S7Frame.cs                    (TPKT/COTP/S7Comm Build+Parse + 地址解析)
│       ├── S7Plugin.cs                            (两个Factory共用一个Driver，TSAP区分机型)
│       └── S7PluginManifest.cs
│
└── CommunicationKernel.Tests                      ✅ 32个测试全通过
```

---

## 三、关键接口契约（实现插件时必须遵守）

### IProtocolDriver
```csharp
Task<OperationResult<byte[]>> BuildReadFrameAsync(string address, int length, CancellationToken ct);
Task<OperationResult<byte[]>> BuildWriteFrameAsync(string address, byte[] payload, CancellationToken ct);
Task<OperationResult<byte[]>> ReadAsync(ITransportClient client, string address, int length, CancellationToken ct);
Task<OperationResult> WriteAsync(ITransportClient client, string address, byte[] payload, CancellationToken ct);
```

### ITransportClient（插件收发字节的唯一途径）
```csharp
Task<OperationResult<byte[]>> SendAndReceiveAsync(byte[] request, CancellationToken ct);
```

### IPluginManifest（每个插件DLL必须有一个实现类）
```csharp
PluginDescriptor Descriptor { get; }
// Descriptor.ApiVersion 必须 == 1 (KernelVersions.PluginApiVersion)
```

### OperationResult<T> 使用约定
```csharp
// Success 标注了 [MemberNotNullWhen(true, nameof(Value))]
// 只要 Success 检查后，Value 访问无需 ! 操作符
OperationResult<byte[]> result = await xxx;
if (!result.Success) return result;              // 失败路径
byte[] data = result.Value;                      // 此处编译器静态知道 Value 非 null
```

---

## 四、协议插件地址格式速查

### Modbus TCP（`modbus-tcp`，端口 502）
| 地址格式 | 含义 |
|----------|------|
| `40001` | 保持寄存器 0，UnitId=1 |
| `1:40001` | 保持寄存器 0，UnitId=1（显式） |
| `coil:5` | 线圈 5，UnitId=1 |
| `00001` | 线圈 0（0xxxx 格式） |
| `2:coil:5` | 线圈 5，UnitId=2 |
- length=1 → FC01（线圈）；length>1 → FC03（保持寄存器）
- 写：1字节线圈→FC05；2字节→FC06；>2字节→FC10

### Panasonic MEWTOCOL TCP（`panasonic-mewtocol-tcp`，端口 9094）
| 地址格式 | 含义 |
|----------|------|
| `DT100` | 数据寄存器 100，站号=1 |
| `01:DT100` | 数据寄存器 100，站号=1（显式） |
| `R10A` | R 区 word10 的第 A（10）位 |
| `R100` | R 区触点 100 |
| `X5` / `Y3` | 外部输入/输出触点 |
| `WR50` / `W50` | 链接继电器 50 |
- length=1 或 IsBit → RCS；否则 → RD（字数=ceil(length/2)）
- 数据字节序：MEWTOCOL 低字节先出，读写均需 SwapBytes

### Siemens S7（`siemens-s7-1200` / `siemens-s7-200smart`，端口 102）
| 地址格式 | 含义 |
|----------|------|
| `DB10.DBW0` | DB10 字偏移 0 |
| `DB10.DBB4` | DB10 字节偏移 4 |
| `DB10.DBD8` | DB10 双字偏移 8 |
| `DB10.0` | DB10 字节偏移 0（无前缀） |
| `MB0` / `MW2` / `MD4` | 位内存字节/字/双字 |
| `IB0` / `QB0` | 输入/输出字节 |
| `VB0` / `VW2` / `VD4` | 200Smart V区 |
| `T0` / `C0` | 定时器/计数器 |
- 握手：COTP CR → CC（[5]=0xD0）→ S7 Setup（[8]=0x03,[17-18]=0x00,[19]=0xF0）
- 1200 TSAP=0x0300；200Smart TSAP=0x0200

---

## 五、已解决的重大 Bug（新会话不要重复）

| 日期 | 问题 | 修复 |
|------|------|------|
| 最新 | `OperationResult<T>.Value` 访问 CS8604/CS8602 | 加 `[MemberNotNullWhen(true,nameof(Value))]` |
| 最新 | S7 Setup Communication 握手必然失败 | `[17]!=0xF0`→检查`[17-18]==0x00`+`[19]==0xF0` |
| 最新 | COTP CC 响应长度检查 `<5` 但访问 `[5]` | 改为 `<6` |
| 最新 | S7Frame DB 地址 fallback `sub.Substring(3)` 越界 | 改为条件判断，无前缀直接用 sub |
| 最新 | WriteScheduler `CurrentCount==0` 判断持锁有竞态 | 改为 `bool acquired` 明确追踪 |

---

## 六、待完成工作清单

### Step-B（进行中）：实现剩余协议插件

#### ✅ 已完成
- `modbus-tcp`：FC01/FC03/FC05/FC06/FC10，MBAP帧，异常码映射
- `panasonic-mewtocol-tcp`：RCS/RD/WCS/WD，BCC，低字节序转换
- `siemens-s7-1200` + `siemens-s7-200smart`：TPKT+COTP+S7Comm，读写握手

#### ⚠️ 待实现
- **Modbus RTU**（`modbus-rtu`，串口/串口服务器）
  - CRC16（多项式 0xA001），追加帧尾
  - 帧边界：3.5 字符时间静默（Transport 层配置）
  - 同 FC01/FC03/FC05/FC06/FC10，站号从地址前缀取
  - 参考：`CommunicationKernel.Plugins.Modbus.Tcp` 的 Internal 工具类

- **Modbus ASCII**（`modbus-ascii`，串口）
  - LRC 校验（所有字节累加取补）
  - 帧格式：`: StationID FunctionCode Data LRC CR LF`
  - 每字节编码为 2 个十六进制 ASCII 字符

### Step-C：传输介质插件（Transport Plugins）
- `CommunicationKernel.Plugins.Transport.Tcp`
  - 封装 `TcpClient`，实现 `ITransportFactory` + `ITransportClient`
  - `SendAndReceiveAsync`：写请求 → 读响应（需知道响应帧边界策略）
  - 帧边界策略：长度前缀（Modbus MBAP）/ CR 定界（MEWTOCOL）/ TPKT 头（S7）
  - 建议：引入 `IFramingStrategy` 接口注入不同帧定界逻辑
- `CommunicationKernel.Plugins.Transport.SerialPort`
  - 封装 `System.IO.Ports.SerialPort`
  - RTU：帧间静默定界；ASCII：CR LF 定界

### Step-D：测试补充
- 各协议 `BuildReadFrameAsync` / `BuildWriteFrameAsync` 帧字节验证
- 地址解析边界（前缀/无前缀/越界/非法）
- 异常码分支（Modbus 01-0B，MEWTOCOL 20-29，S7 错误类/码）
- `WriteScheduler` 并发压测（多 Route 并行，同 Route 串行）

---

## 七、Git 信息
- 仓库：`https://github.com/AMXZzzz/CommunicationKernel`
- Token：（见本地环境变量，不存入版本库）
- 分支：`master`
- 本地路径：`C:\Users\E.T\Desktop\通讯调试工具\CommunicationKernel\`

---

## 八、新会话接手时执行顺序
1. `git pull` 获取最新代码
2. 阅读本文件，确认架构约束与当前状态
3. 继续 Step-B：实现 Modbus RTU → Modbus ASCII
4. 继续 Step-C：实现 Transport.Tcp + Transport.SerialPort（重点：帧边界策略）
5. 继续 Step-D：补充协议帧单元测试
6. 全量构建验证（`TreatWarningsAsErrors=true` 所有项目必须通过）
7. 推送到 GitHub

---

## 九、架构目标（不变）
- 多端 UI（WPF/WebUI/Mac/Android/鸿蒙/Linux）并行接入
- Host 作为统一入口（高性能 gRPC）
- Router 统一并发调度（同路由串行写、同键读合并、跨路由并行）
- Protocol / Transport 抽象解耦，插件 DLL 扩展能力（重启生效）
- 全局严格质量门禁：`TreatWarningsAsErrors=true`，零 nullable 警告
